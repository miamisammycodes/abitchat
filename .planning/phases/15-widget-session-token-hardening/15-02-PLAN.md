---
phase: 15-widget-session-token-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 15-PLAN
gap_closure: true
files_modified:
  - app/Models/Tenant.php
  - app/Http/Middleware/ValidateWidgetDomain.php
  - app/Http/Middleware/CheckUsageLimits.php
  - app/Http/Controllers/Api/V1/Widget/ChatController.php
  - app/Http/Controllers/Api/V1/Widget/LeadController.php
  - app/Http/Controllers/Client/WidgetController.php
  - app/Http/Middleware/RequireWidgetSessionToken.php
  - database/seeders/DatabaseSeeder.php
  - tests/Feature/Widget/TrustProxiesTest.php
  - tests/Feature/Widget/CacheKeyHardeningTest.php
  - tests/Feature/Widget/TenantSavedHookRotationTest.php
autonomous: true
requirements:
  - CONS-22-f
  - CONS-22-g

must_haves:
  truths:
    - "VERIFIED AFTER GAP CLOSURE (SC4): WIDGET_SESSION_DUAL_ACCEPT defaults to false AND strict mode is safe because api_key rotation through ANY path (controller, console, factory, job, direct model save) immediately invalidates the api_key-keyed cache via the Tenant::saved model hook — the old key cannot authenticate even within the 5-minute TTL window"
    - "VERIFIED AFTER GAP CLOSURE (SC5): api_key_hash column exists with unique index AND no raw api_key string appears anywhere in cache storage — all 5 cache call sites (ValidateWidgetDomain, CheckUsageLimits, ChatController get+put, WidgetController forget) key on the SHA-256 hash, so a Redis dump or KEYS scan reveals no plaintext api_keys"
    - "REAFFIRMED (SC1): TrustProxies remains wired with trust-none default; behavioral test now asserts $request->ip() actually returns the X-Forwarded-For value when proxies are trusted and ignores it otherwise (replaces the file_get_contents pseudo-tests)"
    - "REAFFIRMED (SC2): WidgetAudit::log() failures continue to be swallowed via try/catch on both reject and approve paths; widget_audit_failures counter still incremented"
    - "REAFFIRMED (SC3): null api_key still returns structured 401 with SESSION_TOKEN_REQUIRED across the middleware chain"
    - "Single source of truth: hash('sha256', $apiKey . config('app.key')) lives ONLY inside Tenant::hashApiKey(); no other file contains the literal recipe"
    - "Single source of truth: api_key cache invalidation lives ONLY in Tenant::booted() saved hook; WidgetController::regenerateApiKey no longer carries an explicit Cache::forget call"
    - "Constant-time api_key comparison: RequireWidgetSessionToken uses hash_equals(), not !=="
    - "config/widget.php session_dual_accept default and RequireWidgetSessionToken config() fallback both read false (no misleading true fallback)"
  artifacts:
    - path: "app/Models/Tenant.php"
      provides: "Tenant::hashApiKey() canonical helper; saved hook invalidates api_key_hash cache on wasChanged('api_key'); saving hook nulls api_key_hash when api_key is nulled"
      contains: "hashApiKey"
    - path: "app/Http/Middleware/ValidateWidgetDomain.php"
      provides: "Cache key uses tenant:api_key_hash:{hash} via Tenant::hashApiKey()"
      contains: "api_key_hash"
    - path: "app/Http/Middleware/CheckUsageLimits.php"
      provides: "Cache key uses tenant:api_key_hash:{hash} via Tenant::hashApiKey()"
      contains: "api_key_hash"
    - path: "app/Http/Controllers/Api/V1/Widget/ChatController.php"
      provides: "Cache::get/put keys use tenant:api_key_hash:{hash} via Tenant::hashApiKey()"
      contains: "api_key_hash"
    - path: "app/Http/Controllers/Client/WidgetController.php"
      provides: "regenerateApiKey no longer calls Cache::forget — saved hook is the canonical invalidator"
      contains: "regenerateApiKey"
    - path: "app/Http/Middleware/RequireWidgetSessionToken.php"
      provides: "hash_equals constant-time api_key comparison; config('widget.session_dual_accept', false) fallback"
      contains: "hash_equals"
    - path: "tests/Feature/Widget/CacheKeyHardeningTest.php"
      provides: "Regression net proving raw api_key absent from Redis keys"
    - path: "tests/Feature/Widget/TenantSavedHookRotationTest.php"
      provides: "Regression net proving non-controller api_key rotation invalidates cache and old key 401s on next request"
    - path: "tests/Feature/Widget/TrustProxiesTest.php"
      provides: "Behavioral assertion that $request->ip() honors X-Forwarded-For when proxy trusted; replaces file_get_contents pseudo-tests"
  key_links:
    - from: "app/Models/Tenant.php saved hook"
      to: "tenant:api_key_hash:{hash-of-old-key} cache entry"
      via: "Cache::forget(\"tenant:api_key_hash:\" . self::hashApiKey($tenant->getOriginal('api_key')))"
      pattern: "wasChanged\\('api_key'\\)"
    - from: "All cache call sites (5)"
      to: "Tenant::hashApiKey()"
      via: "static method call"
      pattern: "Tenant::hashApiKey"
    - from: "RequireWidgetSessionToken api_key compare"
      to: "constant-time comparison"
      via: "hash_equals()"
      pattern: "hash_equals"

---

<objective>
Close the two BLOCKER gaps identified by the Phase 15 verifier (CR-01 and CR-02) and absorb four cheap WARNING fixes (WR-01, WR-04, WR-05, WR-07) that sit naturally next to the blocker work. After this plan ships, Phase 15 satisfies all 5 ROADMAP success criteria and the strict-mode cutover is safe to declare.

Purpose: The original `15-PLAN.md` shipped on `feat/widget-session-token-hardening` and correctly migrated all 5 DB lookup sites to use `api_key_hash`. But two production-exploitable gaps remain:

- **CR-01 (blocks SC5):** Three independent cache-write sites still key Redis entries on the raw api_key string, exposing every active api_key in plaintext for up to 5 minutes. The blind-index confidentiality goal is undermined by the cache layer even though the database layer is correct.
- **CR-02 (blocks SC4):** The `Tenant::saved` hook only invalidates `tenant:{id}:with_plan`. Any api_key rotation outside `WidgetController::regenerateApiKey` (tinker, factories, jobs, admin tooling) leaves stale tenant data — with the OLD api_key — in cache for ~5 minutes. Strict-mode cutover is not safe to declare because this is an auth-bypass window.

Output: All 5 cache call sites keyed by hash, single canonical `Tenant::hashApiKey()` helper consumed by both the model hooks and the cache call sites, `Tenant::saved` hook invalidates the api_key-keyed cache on rotation, constant-time api_key comparison in middleware, behavioral TrustProxies test, strict_types in seeder, accurate `session_dual_accept` fallback.

Cross-phase note: Phase 14 (Data Encryption at Rest) still depends on the `api_key_hash` column landing — that column is unaffected by this gap plan. The cache layer fix removes the last remaining raw-api_key exposure outside the (Phase-14-targeted) database column.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-widget-session-token-hardening/15-VERIFICATION.md
@.planning/phases/15-widget-session-token-hardening/15-REVIEW.md
@.planning/phases/15-widget-session-token-hardening/15-SUMMARY.md
@app/Models/Tenant.php
@app/Http/Middleware/ValidateWidgetDomain.php
@app/Http/Middleware/CheckUsageLimits.php
@app/Http/Controllers/Api/V1/Widget/ChatController.php
@app/Http/Controllers/Api/V1/Widget/LeadController.php
@app/Http/Controllers/Client/WidgetController.php
@app/Http/Middleware/RequireWidgetSessionToken.php
@config/widget.php
@database/seeders/DatabaseSeeder.php
@tests/Feature/Widget/TrustProxiesTest.php

<interfaces>
<!-- Extracted from the shipped codebase (post-Plan-01). Executor should rely on these without re-exploring. -->

Hash recipe — currently duplicated at 6 sites (confirmed via grep):
- app/Models/Tenant.php:77  — creating hook
- app/Models/Tenant.php:83  — saving hook
- app/Http/Middleware/ValidateWidgetDomain.php:42 (cache-miss filler)
- app/Http/Middleware/CheckUsageLimits.php:77 (cache-miss filler)
- app/Http/Controllers/Api/V1/Widget/ChatController.php:371 (cache-miss filler)
- app/Http/Controllers/Api/V1/Widget/LeadController.php:37 (uncached DB lookup)

Expression at every site: hash('sha256', $apiKey . config('app.key'))
SessionTokenService.php:33 uses the equivalent recipe but with $this->secret (also = config('app.key')) and is the canonical "sub" claim — leave that file alone in this plan; rewriting it would tangle into JWT verification.

Cache-key sites — currently use "tenant:api_key:{$rawApiKey}" (CR-01):
- app/Http/Middleware/ValidateWidgetDomain.php:40 — Cache::remember
- app/Http/Middleware/CheckUsageLimits.php:75 — Cache::remember
- app/Http/Controllers/Api/V1/Widget/ChatController.php:366 — Cache::get
- app/Http/Controllers/Api/V1/Widget/ChatController.php:373 — Cache::put
- app/Http/Controllers/Client/WidgetController.php:78 — Cache::forget (regenerateApiKey)

Tenant model boot hooks (current shape — Tenant.php:66-90):
  creating:   if (empty api_key) Str::random(64); always recompute api_key_hash AFTER assignment
  saving:     if isDirty('api_key') && api_key  → recompute api_key_hash
  saved:      Cache::forget("tenant:{$tenant->id}:with_plan")  — NO api_key cache invalidation (CR-02)

RequireWidgetSessionToken.php:68 (WR-01):
  if ($bodyApiKey !== null && $bodyApiKey !== $tenant->api_key) → return 401

RequireWidgetSessionToken.php:26 (WR-07):
  $dualAccept = (bool) config('widget.session_dual_accept', true);   ← misleading true fallback
config/widget.php:6:
  'session_dual_accept' => env('WIDGET_SESSION_DUAL_ACCEPT', false)   ← config default is correctly false

database/seeders/DatabaseSeeder.php — first 3 lines lack declare(strict_types=1) (WR-04).

tests/Feature/Widget/TrustProxiesTest.php (WR-05): tests 1+3 use file_get_contents(base_path('bootstrap/app.php')) and assertStringContainsString — assert source text, not behavior.

Decisions absorbed from advisor:
1. WidgetController::regenerateApiKey's explicit Cache::forget MUST BE DELETED — the model saved hook becomes the single source of truth (CLAUDE.md "no dual-system support"). This is Task 2's responsibility.
2. The new Tenant::hashApiKey() helper MUST replace the literal recipe inside Tenant::creating AND Tenant::saving hooks too — not just at call sites. The post-task grep gate enforces this.
3. RED tests are merged into each fix task (TDD_MODE=false); no standalone RED task. Each task writes its tests first, then implements, then asserts GREEN.
</interfaces>
</context>

<tasks>

<!-- ═══════════════════════════════════════════════════════
     TASK 1 — CR-01 Fix: Cache-key rename + canonical hash helper
     Addresses: CONS-22-f re-claim (SC5)
     Order: This task MUST land before Task 2. Task 2's saved hook
     will Cache::forget the hash-keyed entry, so the hash-keyed
     pattern must already exist when the hook is added.
     ═══════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task 1: Add Tenant::hashApiKey() canonical helper and rename all cache keys from raw api_key to api_key_hash</name>
  <read_first>
    - .planning/phases/15-widget-session-token-hardening/15-VERIFICATION.md (CR-01 section — the gap source)
    - app/Models/Tenant.php (existing creating/saving/saved hooks — must reuse hash recipe identically)
    - app/Http/Middleware/ValidateWidgetDomain.php lines 30-50
    - app/Http/Middleware/CheckUsageLimits.php lines 60-80
    - app/Http/Controllers/Api/V1/Widget/ChatController.php lines 355-380
    - app/Http/Controllers/Api/V1/Widget/LeadController.php lines 30-45 (not a cache site, but the helper must replace its literal recipe too)
    - app/Http/Controllers/Client/WidgetController.php lines 65-82 (regenerateApiKey — keep the Cache::forget call FOR NOW; Task 2 deletes it. This task only renames its key string to the hash-keyed pattern so the rename is atomic across the codebase.)
    - ./CLAUDE.md (strict_types + Pint + "no dual-system support")
  </read_first>
  <files>
    app/Models/Tenant.php,
    app/Http/Middleware/ValidateWidgetDomain.php,
    app/Http/Middleware/CheckUsageLimits.php,
    app/Http/Controllers/Api/V1/Widget/ChatController.php,
    app/Http/Controllers/Api/V1/Widget/LeadController.php,
    app/Http/Controllers/Client/WidgetController.php,
    tests/Feature/Widget/CacheKeyHardeningTest.php
  </files>
  <action>
Step: Make it work — write the failing regression test first, then make it pass.

Sub-step 1 — write the failing test:
Create `tests/Feature/Widget/CacheKeyHardeningTest.php` (Pest feature test). It MUST start with `<?php` + `declare(strict_types=1);`.

It asserts two things and MUST FAIL against the current codebase:
- After hitting a widget endpoint that triggers tenant lookup (e.g., POST /api/v1/widget/init via the AuthenticatesWidget trait or by setting up the tenant + sending a request through the widget middleware chain), `Cache::has("tenant:api_key:{$tenant->api_key}")` is FALSE — and `Cache::has("tenant:api_key_hash:" . Tenant::hashApiKey($tenant->api_key))` is TRUE. The raw-keyed entry must not be written.
- Negative grep: assert `Redis::keys('tenant:api_key:*')` (or whichever Cache facade equivalent exists; use the `Cache` facade with a tagged check if Redis isn't directly available — the simplest portable check is to inspect both `Cache::has()` calls described above). Cache driver in tests is typically `array` — sufficient for this assertion.

Run: `php artisan test --filter=CacheKeyHardeningTest`. Confirm RED. If GREEN already, abort — the gap is not what the verifier described and the situation needs re-investigation.

Sub-step 2 — add the canonical helper to `app/Models/Tenant.php`:
Add a `public static function hashApiKey(string $apiKey): string` immediately above the `booted()` method. Body: returns `hash('sha256', $apiKey . config('app.key'))`. Add a PHPDoc one-liner: "Canonical recipe for api_key_hash. Single source of truth for the SHA-256 + APP_KEY-pepper derivation used by the column, JWT sub, and all cache keys."

Sub-step 3 — rewrite Tenant model hooks to use the helper:
- `creating` hook line 77: change `$tenant->api_key_hash = hash('sha256', $tenant->api_key.config('app.key'));` → `$tenant->api_key_hash = self::hashApiKey($tenant->api_key);`
- `saving` hook line 83: change `$tenant->api_key_hash = hash('sha256', $tenant->api_key.config('app.key'));` → `$tenant->api_key_hash = self::hashApiKey($tenant->api_key);`
- Do NOT modify the `saved` hook in this task — that's Task 2.

Sub-step 4 — rewrite all 5 cache-key sites to use the helper AND the new key pattern:
At each site, replace `"tenant:api_key:{$apiKey}"` (or `$oldKey`) with `'tenant:api_key_hash:' . Tenant::hashApiKey($apiKey)` (or `$oldKey`). At each cache-miss filler that currently inlines `hash('sha256', $apiKey . config('app.key'))`, replace the literal with `Tenant::hashApiKey($apiKey)`.

Specific edits:

a) `app/Http/Middleware/ValidateWidgetDomain.php` lines 39-43:
   - Cache key: `"tenant:api_key:{$apiKey}"` → `'tenant:api_key_hash:' . Tenant::hashApiKey($apiKey)`
   - Cache-miss filler: `Tenant::where('api_key_hash', hash('sha256', $apiKey.config('app.key')))->first()` → `Tenant::where('api_key_hash', Tenant::hashApiKey($apiKey))->first()`
   - Ensure `use App\Models\Tenant;` import already present (it is — used on line 42 already).

b) `app/Http/Middleware/CheckUsageLimits.php` lines 74-78: identical pattern (cache key + miss filler).

c) `app/Http/Controllers/Api/V1/Widget/ChatController.php` lines 366-373: both `Cache::get(...)` (line 366) and `Cache::put(...)` (line 373) get the hash-keyed pattern; the miss filler at line 371 uses `Tenant::hashApiKey($apiKey)`.

d) `app/Http/Controllers/Api/V1/Widget/LeadController.php` line 37: NOT a cache site, but it inlines the same literal recipe. Replace `hash('sha256', $request->api_key.config('app.key'))` with `Tenant::hashApiKey($request->api_key)`. (LeadController does not key into the cache — leave its uncached behavior alone; Task 2's hook handles cache invariants.)

e) `app/Http/Controllers/Client/WidgetController.php` line 78: rename the Cache::forget key from `"tenant:api_key:{$oldKey}"` to `'tenant:api_key_hash:' . Tenant::hashApiKey($oldKey)`. KEEP the call — Task 2 deletes it. This task's responsibility is the atomic key-string rename only.

Sub-step 5 — verify and commit:
- Run `php artisan test --filter=CacheKeyHardeningTest` — must now be GREEN.
- Run `php artisan test` — full suite green (no regressions on existing widget feature tests).
- Run `./vendor/bin/phpstan analyse --no-progress` — 0 errors.
- Run `./vendor/bin/pint --test`; if anything flagged, `./vendor/bin/pint` then re-run tests.
- Commit: `feat(15-gap): rename widget cache keys to api_key_hash + extract Tenant::hashApiKey helper (CR-01)`
  </action>
  <verify>
    <automated>php artisan test tests/Feature/Widget/CacheKeyHardeningTest.php && php artisan test && ./vendor/bin/phpstan analyse --no-progress && ./vendor/bin/pint --test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "'tenant:api_key:" app/ --include='*.php' | grep -v "^#" | wc -l` returns `0` (the raw-keyed pattern is fully extinct from app/).
    - `grep -rn "tenant:api_key_hash:" app/ --include='*.php' | grep -v "^#" | wc -l` returns `5` or more (4 cache sites — ValidateWidgetDomain, CheckUsageLimits, ChatController twice, WidgetController; plus any new helper-using sites).
    - `grep -n "public static function hashApiKey" app/Models/Tenant.php` returns one match.
    - `grep -c "hash('sha256'" app/Models/Tenant.php` returns `1` (the helper body is the only place the literal lives in this file; the two hook sites now call `self::hashApiKey(...)`).
    - `grep -rn "hash('sha256'.*api_key.*config('app.key')" app/Http app/Models --include='*.php' | grep -v 'Tenant.php' | wc -l` returns `0` (the literal recipe does not appear anywhere outside `Tenant.php`'s helper body).
    - `php artisan test --filter=CacheKeyHardeningTest` exits 0 with at least 1 passing test.
    - Full `php artisan test` exits 0.
    - `./vendor/bin/phpstan analyse --no-progress` reports 0 errors.
    - `./vendor/bin/pint --test` reports clean.
  </acceptance_criteria>
  <done>
    - `Tenant::hashApiKey(string): string` exists and is the single source of truth for the SHA-256+APP_KEY recipe.
    - All 5 cache call sites use `tenant:api_key_hash:{hash}` keys via the helper.
    - Both Tenant model hooks (creating, saving) call `self::hashApiKey(...)` instead of inlining the literal.
    - Regression test `CacheKeyHardeningTest` exists, was RED before this task's implementation, is now GREEN.
    - Full suite green, PHPStan 0, Pint clean.
    - SC5 closure prerequisite met (cache layer no longer carries raw api_keys); CR-01 closed.
  </done>
</task>

<!-- ═══════════════════════════════════════════════════════
     TASK 2 — CR-02 Fix: saved-hook invalidation + WR-02 null branch + delete WidgetController duplicate forget
     Addresses: CONS-22-g re-claim (SC4)
     Depends on Task 1: the hash-keyed pattern must exist before the
     saved hook calls Cache::forget with it. Task 1's edit to
     WidgetController:78 already renamed that key; this task DELETES
     that line because the saved hook is now canonical.
     ═══════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task 2: Add api_key cache invalidation to Tenant saved hook; delete the now-duplicate forget in WidgetController; clear api_key_hash when api_key is nulled</name>
  <read_first>
    - .planning/phases/15-widget-session-token-hardening/15-VERIFICATION.md (CR-02 section — the gap source)
    - app/Models/Tenant.php (current saved/saving hooks, post-Task-1)
    - app/Http/Controllers/Client/WidgetController.php (regenerateApiKey — the explicit forget to be deleted)
    - app/Http/Middleware/RequireWidgetSessionToken.php lines 65-75 (the stale-cache consumer — line 68 compares $bodyApiKey to $tenant->api_key)
    - /Users/sam/.claude/projects/-Users-sam-Dev-laravel-chatbot/memory/tenant_saved_cache.md (prior memory: "single source of truth for tenant:{id}:with_plan invalidation; don't duplicate explicit Cache::forget calls" — extended here to api_key-keyed cache, using wasChanged + getOriginal)
  </read_first>
  <files>
    app/Models/Tenant.php,
    app/Http/Controllers/Client/WidgetController.php,
    tests/Feature/Widget/TenantSavedHookRotationTest.php
  </files>
  <action>
Step: Make it work — write the failing regression test first, then implement.

Sub-step 1 — write the failing test:
Create `tests/Feature/Widget/TenantSavedHookRotationTest.php` (Pest feature test). MUST start with `<?php` + `declare(strict_types=1);`.

The test sequence:
- Create a tenant via the factory; capture `$oldApiKey = $tenant->api_key`.
- Make a widget request using `$oldApiKey` so the cache entry `tenant:api_key_hash:{hash-of-old}` is populated. Verify cache has the entry.
- Rotate the api_key via direct model update (NOT the controller): `$tenant->update(['api_key' => 'rotated-key-' . Str::random(32)])`.
- Assert `Cache::has('tenant:api_key_hash:' . Tenant::hashApiKey($oldApiKey))` is FALSE — the saved hook invalidated it.
- Make a second widget request using `$oldApiKey`. Assert response status 401 (or the body-api_key compare returns the SESSION_EXPIRED error). Currently — without the hook fix — this would 200 because the stale cached tenant still authenticates via `$bodyApiKey !== $tenant->api_key` where both sides are the old value.

Also add a second test for WR-02 null-branch behavior:
- Create a tenant. Capture `$tenant->api_key_hash` is non-null.
- `$tenant->update(['api_key' => null])`.
- Refresh. Assert `$tenant->api_key_hash === null`. (Currently fails — the saving hook only sets the hash when `$tenant->api_key` is truthy, leaving stale hash.)

Run: `php artisan test --filter=TenantSavedHookRotationTest`. Confirm RED on both tests.

Sub-step 2 — extend Tenant `saved` hook to invalidate api_key cache on rotation (`app/Models/Tenant.php`):
Inside the existing `static::saved(...)` closure, add (after the existing `Cache::forget("tenant:{$tenant->id}:with_plan")` line):

Pseudo-spec (executor: emit valid PHP, use existing `use Illuminate\Support\Facades\Cache;` import which is already present at top of file):
- If `$tenant->wasChanged('api_key')`: derive `$oldKey = $tenant->getOriginal('api_key')`. If `$oldKey` is non-empty, call `Cache::forget('tenant:api_key_hash:' . self::hashApiKey($oldKey))`. **Use `getOriginal('api_key')` — not the current value — because the cache slot to evict is keyed on the PREVIOUS api_key's hash. The new api_key's slot is empty and will hydrate on next request; forgetting the current value would no-op against an empty slot and leave the old slot alive (the exact bug the verifier described).**

Sub-step 3 — extend Tenant `saving` hook to clear api_key_hash when api_key is nulled (WR-02):
Current code: `if ($tenant->isDirty('api_key') && $tenant->api_key) { $tenant->api_key_hash = self::hashApiKey($tenant->api_key); }`.
Change to: `if ($tenant->isDirty('api_key')) { $tenant->api_key_hash = $tenant->api_key ? self::hashApiKey($tenant->api_key) : null; }`.

Sub-step 4 — delete the now-duplicate forget in WidgetController (`app/Http/Controllers/Client/WidgetController.php`):
Lines 71-78 currently:
```
$tenant = $this->getTenant($request);
$oldKey = $tenant->api_key;
$tenant->update(['api_key' => bin2hex(random_bytes(32))]);
Cache::forget('tenant:api_key_hash:' . Tenant::hashApiKey($oldKey));   // ← Task 1 renamed this
```

Delete the `$oldKey = $tenant->api_key;` line and the `Cache::forget(...)` line. The saved hook now owns invalidation; the controller path is just one more rotation site that the hook covers uniformly. Per CLAUDE.md "no dual-system support — choose one and commit."

If the `use Illuminate\Support\Facades\Cache;` import becomes unused after the deletion, remove it. Run a quick `grep -n 'Cache::' app/Http/Controllers/Client/WidgetController.php` to confirm before deleting the import.

Sub-step 5 — verify and commit:
- Run `php artisan test --filter=TenantSavedHookRotationTest` — both tests GREEN.
- Run `php artisan test --filter=CacheKeyHardeningTest` — still GREEN (no regression).
- Run full `php artisan test` — green.
- Run `./vendor/bin/phpstan analyse --no-progress` — 0 errors.
- Run `./vendor/bin/pint --test` clean.
- Commit: `feat(15-gap): invalidate api_key cache on rotation via saved hook; drop duplicate forget; null api_key clears hash (CR-02, WR-02)`
  </action>
  <verify>
    <automated>php artisan test tests/Feature/Widget/TenantSavedHookRotationTest.php tests/Feature/Widget/CacheKeyHardeningTest.php && php artisan test && ./vendor/bin/phpstan analyse --no-progress && ./vendor/bin/pint --test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "wasChanged('api_key')" app/Models/Tenant.php` returns one match (inside saved hook).
    - `grep -n "getOriginal('api_key')" app/Models/Tenant.php` returns one match (inside saved hook).
    - `grep -c "Cache::forget" app/Models/Tenant.php` returns `2` (one for `with_plan`, one for `api_key_hash`).
    - `grep -n "Cache::forget" app/Http/Controllers/Client/WidgetController.php` returns zero matches (duplicate deleted).
    - `php artisan test --filter=TenantSavedHookRotationTest` exits 0 with at least 2 passing tests (rotation invalidates cache; null api_key clears hash).
    - `php artisan test --filter=CacheKeyHardeningTest` still exits 0 (Task 1 regression net intact).
    - Full `php artisan test` exits 0.
    - `./vendor/bin/phpstan analyse --no-progress` reports 0 errors.
    - `./vendor/bin/pint --test` reports clean.
  </acceptance_criteria>
  <done>
    - Tenant `saved` hook invalidates `tenant:api_key_hash:{hash-of-OLD-key}` on `wasChanged('api_key')`.
    - Tenant `saving` hook nulls `api_key_hash` when `api_key` is set to null (WR-02).
    - `WidgetController::regenerateApiKey` no longer carries an explicit `Cache::forget`; saved hook is canonical.
    - Regression test `TenantSavedHookRotationTest` covers non-controller rotation and null api_key paths.
    - Full suite green, PHPStan 0, Pint clean.
    - SC4 closure met: strict-mode cutover is safe — old api_key cannot authenticate across the 5-minute TTL window from any rotation path.
    - CR-02 closed.
  </done>
</task>

<!-- ═══════════════════════════════════════════════════════
     TASK 3 — Hardening absorbs: WR-01, WR-04, WR-05, WR-07
     Addresses: code-review WARNINGs that sit naturally adjacent
     to the gap work; cheap one-liners + one test rewrite.
     ═══════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task 3: Constant-time api_key compare + accurate session_dual_accept fallback + seeder strict_types + behavioral TrustProxies test</name>
  <read_first>
    - .planning/phases/15-widget-session-token-hardening/15-REVIEW.md (WR-01, WR-04, WR-05, WR-07 sections)
    - app/Http/Middleware/RequireWidgetSessionToken.php (lines 22-30 for config fallback; lines 65-75 for the comparison)
    - tests/Feature/Widget/TrustProxiesTest.php (existing — to be rewritten; lines 30-69 are the file_get_contents pseudo-tests)
    - database/seeders/DatabaseSeeder.php (line 1 — missing strict_types)
    - config/widget.php (line 6 — for confirming the canonical default)
    - ./CLAUDE.md (strict_types mandate)
  </read_first>
  <files>
    app/Http/Middleware/RequireWidgetSessionToken.php,
    database/seeders/DatabaseSeeder.php,
    tests/Feature/Widget/TrustProxiesTest.php
  </files>
  <action>
Step: Make it work — make each one-line fix, then rewrite the TrustProxies tests to assert behavior.

Sub-step 1 — WR-01: constant-time api_key comparison (`app/Http/Middleware/RequireWidgetSessionToken.php` line 68):
Current:
  `if ($bodyApiKey !== null && $bodyApiKey !== $tenant->api_key) {`
Change to:
  `if ($bodyApiKey !== null && ! hash_equals((string) $tenant->api_key, (string) $bodyApiKey)) {`
The cast to string is defensive — `api_key` is a string column but PHP doesn't statically prove that to the comparison, and `hash_equals` requires both args be strings.

Sub-step 2 — WR-07: fix misleading config fallback (`app/Http/Middleware/RequireWidgetSessionToken.php` line 26):
Current:
  `$dualAccept = (bool) config('widget.session_dual_accept', true);`
Change to:
  `$dualAccept = (bool) config('widget.session_dual_accept', false);`
This matches `config/widget.php` line 6 (`env('WIDGET_SESSION_DUAL_ACCEPT', false)`). If `config/widget.php` is ever absent, the fallback is now consistent — strict-mode-by-default, which is the secure default.

Sub-step 3 — WR-04: add `declare(strict_types=1);` to `database/seeders/DatabaseSeeder.php`:
Insert `declare(strict_types=1);` (followed by a blank line) immediately after the opening `<?php` tag, before the `namespace` line. CLAUDE.md mandates strict_types in every PHP file; this seeder was edited in Phase 15 Plan 01 (it sets api_key_hash explicitly per the WithoutModelEvents deviation) so this is a regression introduced by Plan 01, not pre-existing debt.

Sub-step 4 — WR-05: rewrite TrustProxies tests to assert behavior, not source text (`tests/Feature/Widget/TrustProxiesTest.php`):
- Delete the existing tests that use `file_get_contents(base_path('bootstrap/app.php'))` and `assertStringContainsString` (the WR-05 finding: those tests pass for any file containing the literal strings, including commented-out code — they assert nothing about runtime behavior).
- Replace with two behavioral tests using `\Illuminate\Http\Request::setTrustedProxies(...)` and an ad-hoc test route that returns `$request->ip()`:

Test A — `test_x_forwarded_for_is_honored_when_remote_addr_is_trusted`:
- Call `\Illuminate\Http\Request::setTrustedProxies(['127.0.0.1'], \Illuminate\Http\Request::HEADER_X_FORWARDED_FOR);` at the start of the test.
- Register an ad-hoc route inside the test: `Route::get('/__test_ip', fn (\Illuminate\Http\Request $r) => ['ip' => $r->ip()]);`.
- `$this->withHeaders(['X-Forwarded-For' => '1.2.3.4'])->get('/__test_ip')->assertOk()->assertJson(['ip' => '1.2.3.4']);`

Test B — `test_x_forwarded_for_is_ignored_when_remote_addr_is_not_trusted`:
- Call `\Illuminate\Http\Request::setTrustedProxies([], \Illuminate\Http\Request::HEADER_X_FORWARDED_FOR);`.
- Same ad-hoc route as Test A.
- `$this->withHeaders(['X-Forwarded-For' => '1.2.3.4'])->get('/__test_ip');` — assert the response JSON's `ip` field is NOT `1.2.3.4` (`assertJsonMissing(['ip' => '1.2.3.4'])` is the cleanest; or assert the value equals `127.0.0.1` which is the test runner's REMOTE_ADDR).

Both tests use `Route::get(...)` inside the test (or via `setUp`); ensure `use Illuminate\Support\Facades\Route;` is imported.

Keep any other existing TrustProxies tests that DO assert behavior (e.g., a test that verifies the rate-limit middleware key uses the forwarded IP). Only delete the ones that grep `bootstrap/app.php` source text.

Sub-step 5 — verify and commit:
- Run `php artisan test --filter=TrustProxiesTest` — new tests pass.
- Run `php artisan test` — full suite green.
- Run `./vendor/bin/phpstan analyse --no-progress` — 0 errors.
- Run `./vendor/bin/pint --test` clean.
- Commit: `fix(15-gap): hash_equals api_key compare + accurate session_dual_accept fallback + seeder strict_types + behavioral TrustProxies test (WR-01,04,05,07)`
  </action>
  <verify>
    <automated>php artisan test tests/Feature/Widget/TrustProxiesTest.php && php artisan test && ./vendor/bin/phpstan analyse --no-progress && ./vendor/bin/pint --test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "hash_equals" app/Http/Middleware/RequireWidgetSessionToken.php` returns at least one match (api_key compare line 68 area).
    - `grep -n "!== \$tenant->api_key" app/Http/Middleware/RequireWidgetSessionToken.php` returns zero matches (the non-constant-time compare is gone).
    - `grep -n "session_dual_accept', false" app/Http/Middleware/RequireWidgetSessionToken.php` returns one match (the fallback now reads `false`).
    - `grep -n "session_dual_accept', true" app/Http/Middleware/RequireWidgetSessionToken.php` returns zero matches.
    - `head -3 database/seeders/DatabaseSeeder.php` shows `<?php` on line 1 and `declare(strict_types=1);` on line 3 (or wherever — must be present before the namespace declaration).
    - `grep -n "file_get_contents.*bootstrap/app.php" tests/Feature/Widget/TrustProxiesTest.php` returns zero matches (the source-text tests are deleted).
    - `grep -n "setTrustedProxies" tests/Feature/Widget/TrustProxiesTest.php` returns at least 2 matches (Test A and Test B both configure the proxy state).
    - `php artisan test --filter=TrustProxiesTest` exits 0 with at least 2 passing tests.
    - Full `php artisan test` exits 0.
    - `./vendor/bin/phpstan analyse --no-progress` reports 0 errors.
    - `./vendor/bin/pint --test` reports clean.
  </acceptance_criteria>
  <done>
    - api_key comparison in RequireWidgetSessionToken uses `hash_equals` (constant-time).
    - `session_dual_accept` config fallback in middleware reads `false`, matching `config/widget.php`.
    - `database/seeders/DatabaseSeeder.php` declares strict_types.
    - TrustProxies tests assert `$request->ip()` resolution behavior (X-Forwarded-For honored when trusted, ignored when not), not source text presence.
    - Full suite green, PHPStan 0, Pint clean.
    - All four warnings (WR-01, WR-04, WR-05, WR-07) closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Widget JS → /api/v1/widget/* | Untrusted HTTP input (api_key, origin, JWT bearer, body fields) — unchanged from Plan 01 |
| Redis cache layer | Internal store; CR-01 closure removes raw api_key persistence here |
| Model write path → cache | CR-02 closure: every api_key rotation (any path) invalidates the api_key-keyed cache via the saved hook |
| APP_KEY → hash derivation | Single canonical helper `Tenant::hashApiKey` replaces 6 inline call sites |

## STRIDE Threat Register (incremental — supplements Plan 01's table)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-08 | Information Disclosure | Redis cache keys carrying raw api_key (CR-01) | mitigate | Task 1 renames all 5 cache key sites to `tenant:api_key_hash:{hash}`; recipe centralized in `Tenant::hashApiKey()`; grep gate enforces zero `tenant:api_key:` strings remain in `app/` |
| T-15-09 | Elevation of Privilege | Stale-cache auth-bypass window after non-controller api_key rotation (CR-02) | mitigate | Task 2 adds `wasChanged('api_key')` branch to `Tenant::saved` hook that `Cache::forget`s the previous key's hash-keyed slot using `getOriginal('api_key')`; covers tinker, factory, job, admin, and direct model save paths uniformly |
| T-15-10 | Information Disclosure | Timing side-channel on api_key compare (WR-01) | mitigate | Task 3 replaces `!==` with `hash_equals` for the body-api_key vs tenant-api_key comparison in `RequireWidgetSessionToken` |
| T-15-11 | Repudiation | Misleading `session_dual_accept` true fallback (WR-07) | mitigate | Task 3 changes middleware config fallback from `true` to `false`, matching the canonical default in `config/widget.php` — fail-secure if config is ever absent |
| T-15-12 | Tampering | Stale `api_key_hash` after `api_key` is nulled (WR-02) | mitigate | Task 2 extends saving hook so `isDirty('api_key')` + null api_key sets `api_key_hash` to null — no orphaned hash continues to resolve to the tenant |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this gap plan (no audit required) |
</threat_model>

<verification>
## Phase 15 Gap-Closure Overall Verification

After all 3 tasks complete:

```bash
# 1. Zero raw-api_key cache keys remain
grep -rn "'tenant:api_key:" app/ --include='*.php' | grep -v "^#" | wc -l
# Expected: 0

# 2. The canonical helper exists and is the only place the recipe lives
grep -n "public static function hashApiKey" app/Models/Tenant.php
# Expected: 1 match
grep -rn "hash('sha256'.*api_key.*config('app.key')" app/Http app/Models --include='*.php' | grep -v 'Tenant.php' | wc -l
# Expected: 0  (recipe lives only inside Tenant.php's helper body)

# 3. saved hook covers api_key rotation
grep -n "wasChanged('api_key')" app/Models/Tenant.php
# Expected: 1 match
grep -n "getOriginal('api_key')" app/Models/Tenant.php
# Expected: 1 match

# 4. WidgetController no longer carries the duplicate forget
grep -n "Cache::forget" app/Http/Controllers/Client/WidgetController.php
# Expected: 0 matches

# 5. Constant-time compare
grep -n "hash_equals" app/Http/Middleware/RequireWidgetSessionToken.php
# Expected: at least 1 (for the api_key compare)

# 6. session_dual_accept fallback matches config
grep -n "session_dual_accept', true" app/Http/Middleware/RequireWidgetSessionToken.php
# Expected: 0 matches

# 7. Seeder declares strict_types
grep -n "declare(strict_types=1)" database/seeders/DatabaseSeeder.php
# Expected: 1 match

# 8. Full test suite (must stay green)
php artisan test
# Expected: all passing, no skipped beyond the existing 1

# 9. PHPStan zero baseline (DEC-09)
./vendor/bin/phpstan analyse --no-progress
# Expected: 0 errors

# 10. Pint clean
./vendor/bin/pint --test
# Expected: clean
```

**Smoke test (manual, against http://127.0.0.1:8001):**
1. `php artisan tinker` → `$t = App\Models\Tenant::first(); $old = $t->api_key; $t->update(['api_key' => 'new-test-key-' . Str::random(16)]);`
2. `curl -X POST http://127.0.0.1:8001/api/v1/widget/init -H 'Origin: <tenant-domain>' -d "api_key=$old"` — expect 401 (old api_key no longer authenticates).
3. Confirm widget on `http://127.0.0.1:8001/widget/test.html` still loads and accepts messages after rotation (refresh required to pick up new key).
</verification>

<success_criteria>
## ROADMAP Success Criteria Closure (after gap plan)

| SC# | Criterion | Plan-01 Status | Gap Plan Closure |
|-----|-----------|----------------|------------------|
| SC1 | TrustProxies configured; per-IP rate limits + IP-binding through proxies | VERIFIED | Reaffirmed by Task 3's behavioral TrustProxies test (replaces source-text pseudo-tests) |
| SC2 | WidgetAudit::log() failures swallowed | VERIFIED | Unchanged — try/catch on both reject and approve paths preserved |
| SC3 | Null api_key returns structured 401 | VERIFIED | Unchanged — middleware chain still returns SESSION_TOKEN_REQUIRED |
| SC4 | WIDGET_SESSION_DUAL_ACCEPT=false default; strict mode safe | **CLOSED BY TASK 2** | `Tenant::saved` invalidates api_key cache on rotation; old key cannot authenticate from any rotation path; strict mode safe to declare |
| SC5 | api_key_hash has DB index; raw api_key not in storage/cache | **CLOSED BY TASK 1** | All 5 cache call sites keyed on `api_key_hash`; no raw api_key in Redis; CONS-22-f fully satisfied |

CONS-22-f and CONS-22-g move from PARTIALLY SATISFIED → SATISFIED.
</success_criteria>

## Multi-Source Coverage Audit

| Source | Item | Covered By |
|--------|------|------------|
| VERIFICATION (CR-01) | Cache keys carrying raw api_key | Task 1 |
| VERIFICATION (CR-02) | Tenant saved hook missing api_key cache invalidation | Task 2 |
| REVIEW (WR-01) | Non-constant-time api_key compare | Task 3 |
| REVIEW (WR-02) | saving hook leaves stale hash on null api_key | Task 2 (absorbed alongside saved-hook edit) |
| REVIEW (WR-04) | DatabaseSeeder missing strict_types | Task 3 |
| REVIEW (WR-05) | TrustProxies tests assert source text not behavior | Task 3 |
| REVIEW (WR-06) | WidgetController:78 raw-key forget | Task 1 (renames key) + Task 2 (deletes line) |
| REVIEW (WR-07) | Misleading session_dual_accept=true fallback | Task 3 |
| CLAUDE.md | No dual-system support / single source of truth | Tasks 1 + 2 (helper + saved hook) |
| CONS-22-f re-claim | Indexed api_key_hash; raw api_key not in storage/cache | Task 1 |
| CONS-22-g re-claim | Strict mode safe (no stale-cache auth window) | Task 2 |
| DEC-09 | PHPStan baseline = zero | Enforced in every task's verify step |
| DEC-05 | forTenant / BelongsToTenant (no raw tenant_id) | Unchanged — api_key_hash lookups are not tenant-scoped queries |

**Out of scope for this gap plan (and why):**
- **WR-03** (migration backfill non-transactional, index-before-backfill) — the migration shipped in Plan 01. Fixing a shipped migration in place is risky; if a future deploy fails, address with a NEW migration then. (Recommendation: defer.)
- **REVIEW IN-01** (APP_KEY-empty handling consistency across SessionTokenService / Tenant hook / WidgetAudit) — informational only, not flagged as a SC blocker; would require boot-time validation refactor across three subsystems; defer to a future ops/hardening phase.
- **REVIEW IN-02** (`captureLeadFromMessage` O(n²) per-turn rescan) — informational only; out of phase scope (chat-performance, not widget-hardening).
- **REVIEW WR-06** (`TenantApiKeyHashTest::test_migration_backfill_populates_hash_for_existing_tenants` does not test the migration) — test-quality issue, not a SC blocker; the regression net for the migration's backfill is genuinely thin but addressing it would require designing a way to exercise the migration's chunked backfill closure outside of `migrate:fresh` — defer to a test-improvement pass.

<output>
Create `.planning/phases/15-widget-session-token-hardening/15-02-SUMMARY.md` when done.
</output>
</content>
</invoke>