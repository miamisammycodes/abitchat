---
phase: 16.1-role-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Enums/Role.php
  - app/Enums/Ability.php
  - app/Auth/RolePermissions.php
  - tests/Unit/Enums/RoleTest.php
  - tests/Unit/Enums/AbilityTest.php
  - tests/Unit/Auth/RolePermissionsTest.php
  - tests/Unit/Auth/BotBrainInvariantTest.php
  - tests/Unit/Models/UserRoleTest.php
  - tests/Unit/Models/UserRoleResolutionTest.php
  - tests/Feature/Auth/GateRegistrationTest.php
  - tests/Feature/Auth/SingleGuardTest.php
  - tests/Feature/Auth/LoginRedirectTest.php
  - tests/Feature/Auth/RegisterControllerTest.php
  - tests/Feature/Auth/RoleMatrixTest.php
  - tests/Feature/Auth/ChooseRoleTest.php
  - tests/Feature/Admin/RouteGuardTest.php
  - tests/Feature/Client/AbilityCoverageTest.php
  - tests/Feature/Inertia/AuthShareTest.php
  - tests/Feature/Policies/ConversationPolicyTest.php
  - tests/Feature/Policies/LeadPolicyTest.php
  - tests/Feature/Policies/KnowledgeItemPolicyTest.php
  - tests/Feature/Policies/TransactionPolicyTest.php
  - tests/Feature/Webhook/StripeAnonymousTest.php
  - tests/Concerns/SeedsRoleMatrix.php
autonomous: true
requirements:
  - D-01
  - D-02
  - D-04
  - D-12
  - D-21

must_haves:
  truths:
    - "App\\Enums\\Role is a string-backed enum with cases SuperAdmin, Owner, Manager, Agent and value 'super_admin'|'owner'|'manager'|'agent'"
    - "App\\Enums\\Role::rank() returns Owner=3, Manager=2, Agent=1, SuperAdmin=0 (orthogonal)"
    - "App\\Enums\\Role::label() returns 'Platform Admin' for SuperAdmin, 'Owner'|'Manager'|'Agent' otherwise"
    - "App\\Enums\\Ability is a string-backed enum exposing all 13 ability slugs from the canonical matrix"
    - "App\\Auth\\RolePermissions::MIN_ROLE maps every Ability case to a minimum Role (no missing keys)"
    - "App\\Auth\\RolePermissions::can(User, Ability, ?Tenant) returns true when the role tier is satisfied for the (tenant or platform) scope, false otherwise"
    - "RolePermissions branches SuperAdmin abilities to exact-match hasRole(SuperAdmin); tenant abilities to hasRoleAtLeast(min, tenant)"
    - "Every Wave 0 test stub file exists with namespace + class skeleton + at least one placeholder test (skipped or trivial assertion)"
  artifacts:
    - path: "app/Enums/Role.php"
      provides: "Backed string enum + rank()/isPlatformLevel()/label() helpers"
      contains: "enum Role: string"
    - path: "app/Enums/Ability.php"
      provides: "Backed string enum with 13 ability cases"
      contains: "enum Ability: string"
    - path: "app/Auth/RolePermissions.php"
      provides: "final class RolePermissions { ::can(); ::allAbilities(); }"
      contains: "final class RolePermissions"
    - path: "tests/Concerns/SeedsRoleMatrix.php"
      provides: "actingAsSuperAdmin / actingAsOwner / actingAsManager / actingAsAgent helpers"
      contains: "trait SeedsRoleMatrix"
  key_links:
    - from: "app/Auth/RolePermissions.php"
      to: "app/Enums/Role.php and app/Enums/Ability.php"
      via: "private const MIN_ROLE = [Ability::X->value => Role::Y]"
      pattern: "Ability::[A-Za-z]+->value\\s*=>\\s*Role::[A-Za-z]+"
    - from: "tests/Concerns/SeedsRoleMatrix.php"
      to: "App\\Models\\User and App\\Models\\UserRole"
      via: "User::create + UserRole::create within helpers"
      pattern: "actingAs(SuperAdmin|Owner|Manager|Agent)"
---

<objective>
Lay the static foundation of Phase 16.1: ship the two backed enums (Role + Ability), the RolePermissions decision engine, and the complete Wave 0 test-file scaffold so every downstream plan has a real `<automated>` command to run against.

Purpose: Enums + RolePermissions are referenced by every later wave (model methods, Gate closures, policies, Inertia share). Scaffolding all Wave 0 test stubs in one plan (rather than distributing them across plans) keeps the validation matrix aligned with 16.1-VALIDATION.md and means every later task ships with a real `php artisan test --filter=X` target.

Output: 3 production PHP files + 19 test stub files + 1 test trait.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16.1-role-foundation/16.1-CONTEXT.md
@.planning/phases/16.1-role-foundation/16.1-RESEARCH.md
@.planning/phases/16.1-role-foundation/16.1-PATTERNS.md
@.planning/phases/16.1-role-foundation/16.1-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Existing project enum shape (the canonical analog). Use this header + class body shape for Role and Ability. -->

From app/Enums/KnowledgeItemStatus.php:
```php
<?php

declare(strict_types=1);

namespace App\Enums;

enum KnowledgeItemStatus: string
{
    case Pending = 'pending';
    case Processing = 'processing';
    case Ready = 'ready';
    case Failed = 'failed';
}
```

The full canonical Role + Ability + RolePermissions shapes — including `rank()`, `isPlatformLevel()`, `label()` methods on Role, the 13 Ability cases, and the `MIN_ROLE` const map on RolePermissions — are spelled out verbatim in `16.1-RESEARCH.md` "Pattern 1", "Pattern 2", and "Pattern 3". Executor uses those snippets directly; no improvisation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship App\Enums\Role and App\Enums\Ability backed enums</name>
  <files>app/Enums/Role.php, app/Enums/Ability.php, tests/Unit/Enums/RoleTest.php, tests/Unit/Enums/AbilityTest.php</files>
  <read_first>
    - app/Enums/KnowledgeItemStatus.php (canonical project enum shape — header, namespace, declare(strict_types=1), backed string enum body)
    - .planning/phases/16.1-role-foundation/16.1-PATTERNS.md §"app/Enums/Role.php" + §"app/Enums/Ability.php"
    - .planning/phases/16.1-role-foundation/16.1-RESEARCH.md §"Pattern 1: Backed PHP Enum for Roles" + §"Pattern 2: Backed PHP Enum for Abilities"
  </read_first>
  <behavior>
    - Role enum has 4 cases with backed string values: super_admin, owner, manager, agent
    - Role::rank() returns Owner=3, Manager=2, Agent=1, SuperAdmin=0
    - Role::isPlatformLevel() returns true ONLY for SuperAdmin
    - Role::label() returns 'Platform Admin' for SuperAdmin (never 'Super Admin'), 'Owner'/'Manager'/'Agent' otherwise
    - Ability enum has exactly 13 cases (3 SuperAdmin + 4 Owner + 3 Manager+ + 3 Agent+) with kebab-case backed values from RESEARCH Pattern 2
    - Ability::cases() returns the 13 cases in the order declared in Pattern 2
  </behavior>
  <action>
    Create `app/Enums/Role.php` matching the shape in PATTERNS.md and RESEARCH Pattern 1. Per D-04 the values are `super_admin`, `owner`, `manager`, `agent`. Per D-02 + the orchestrator's UI label rule, `SuperAdmin->label()` returns "Platform Admin" (not "Super Admin"). The `rank()` method returns the ordinal hierarchy (Owner=3, Manager=2, Agent=1, SuperAdmin=0 — orthogonal). Add `isPlatformLevel(): bool` that returns true only for SuperAdmin. All methods declare typed return values. Per D-21 this file must pass Larastan baseline=0 — no missing return types, no string magic.

    Create `app/Enums/Ability.php` with the 13 cases enumerated in RESEARCH Pattern 2. The case names are PascalCase, values are kebab-case slugs (e.g. `ManageBilling = 'manage-billing'`). These 13 slugs become the Gate names verbatim via `->value` per D-10.

    Write `tests/Unit/Enums/RoleTest.php` (Pest) covering: every case has the expected backed value (D-01); rank() ordering (Owner > Manager > Agent) and SuperAdmin orthogonal=0 (D-02); label() returns "Platform Admin" for SuperAdmin (not "Super Admin"); isPlatformLevel() boolean.

    Write `tests/Unit/Enums/AbilityTest.php` covering: count(Ability::cases()) === 13 (D-12 exhaustiveness); every expected slug is present; each value is kebab-case.

    Per CLAUDE.md: declare(strict_types=1) on every new file.
  </action>
  <verify>
    <automated>php artisan test --filter='RoleTest|AbilityTest' --stop-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter='RoleTest|AbilityTest'` exits 0
    - `grep -c "case SuperAdmin = 'super_admin'" app/Enums/Role.php` equals 1
    - `grep -c "case ManageBilling = 'manage-billing'" app/Enums/Ability.php` equals 1
    - `grep -c "Super Admin" app/Enums/Role.php` equals 0 (UI label is "Platform Admin", never "Super Admin")
    - `grep -E "case [A-Z][A-Za-z]+\\s*=\\s*'[a-z-]+'" app/Enums/Ability.php | wc -l` equals 13
    - `./vendor/bin/phpstan analyse app/Enums/Role.php app/Enums/Ability.php --no-progress` reports 0 errors
    - `./vendor/bin/pint --test app/Enums/Role.php app/Enums/Ability.php` reports clean
  </acceptance_criteria>
  <done>Both enums exist with all required cases + methods, both unit tests pass, PHPStan reports 0 errors on the new files, Pint reports no style violations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship App\Auth\RolePermissions decision engine</name>
  <files>app/Auth/RolePermissions.php, tests/Unit/Auth/RolePermissionsTest.php, tests/Unit/Auth/BotBrainInvariantTest.php</files>
  <read_first>
    - .planning/phases/16.1-role-foundation/16.1-RESEARCH.md §"Pattern 3: Canonical RolePermissions Class" (read the entire pattern verbatim — it is the spec)
    - .planning/phases/16.1-role-foundation/16.1-PATTERNS.md §"app/Auth/RolePermissions.php"
    - .planning/phases/16.1-role-foundation/16.1-CONTEXT.md §"Permission Matrix (canonical, shipped in 16.1)" (D-09..D-12)
  </read_first>
  <behavior>
    - RolePermissions::can(User, Ability, ?Tenant) returns true if-and-only-if the user holds the minimum role for the ability
    - For SuperAdmin abilities (view-admin-dashboard, manage-tenant-as-admin, manage-platform-settings), `can()` checks hasRole(Role::SuperAdmin) with no tenant requirement
    - For tenant abilities, `can()` requires `$tenant !== null` AND `hasRoleAtLeast($minRole, $tenant)`
    - Owner-only abilities (manage-billing, manage-team, manage-tenant-settings, delete-tenant): only Owner passes; Manager and Agent fail
    - Manager+ abilities (manage-knowledge-base, manage-integrations, view-analytics-full): Owner AND Manager pass; Agent fails
    - Agent+ abilities (manage-conversations, manage-leads, view-dashboard): Owner AND Manager AND Agent pass
    - allAbilities() returns Ability::cases() (used by HandleInertiaRequests later)
    - BotBrainInvariantTest grep-asserts that no Client FormRequest validates `bot_custom_instructions` (D-09)
  </behavior>
  <action>
    Create `app/Auth/RolePermissions.php` as a `final class` per RESEARCH Pattern 3. The class is `final` (no subclassing), namespace `App\Auth`, declare(strict_types=1). Define `private const MIN_ROLE` mapping every `Ability->value` to a `Role` per D-09..D-12 matrix:

    SuperAdmin-only abilities (3): ViewAdminDashboard, ManageTenantAsAdmin (covers bot_custom_instructions per D-09), ManagePlatformSettings.
    Owner-only abilities (4): ManageBilling, ManageTeam, ManageTenantSettings, DeleteTenant.
    Manager+ abilities (3): ManageKnowledgeBase, ManageIntegrations, ViewAnalyticsFull.
    Agent+ abilities (3): ManageConversations, ManageLeads, ViewDashboard.

    Implement `public static function can(User $user, Ability $ability, ?Tenant $tenant = null): bool` exactly per Pattern 3:
    - Look up `$minRole` from MIN_ROLE.
    - If `$minRole === Role::SuperAdmin`, return `$user->hasRole(Role::SuperAdmin)` ignoring tenant.
    - Otherwise return `$tenant !== null && $user->hasRoleAtLeast($minRole, $tenant)`.

    Implement `public static function allAbilities(): array` returning `Ability::cases()`. Add PHPDoc `@return list<Ability>` so the Inertia share consumer (Plan 05) types correctly.

    Note: User::hasRole / User::hasRoleAtLeast are added in Plan 02 (this plan ships against an interface that Plan 02 implements). Use the signatures from RESEARCH Pattern 5: `hasRole(Role $role, ?Tenant $tenant = null): bool`; `hasRoleAtLeast(Role $min, Tenant $tenant): bool`.

    Test plan for `tests/Unit/Auth/RolePermissionsTest.php`: use a `@dataProvider` to enumerate the full 13×4 = 52-cell matrix and assert can() returns the expected boolean for each cell. Use the SeedsRoleMatrix trait from Task 4 (this test will initially be marked `->skip()` and unskipped after Plan 02 ships User::hasRole). For now, the file ships with at least 4 trivial assertions (one per role) that exercise the const map without requiring User::hasRole — assert that `RolePermissions::allAbilities()` returns 13 items and that `MIN_ROLE` (via reflection) has 13 entries.

    Test plan for `tests/Unit/Auth/BotBrainInvariantTest.php` (D-09 guard test): grep every `app/Http/Requests/Client/**/*.php` file and assert NONE references `bot_custom_instructions` in rules() or as a validated field. Implement with `glob` + `file_get_contents` + `assertStringNotContainsString` looping over each file. This is the guardrail that prevents future tenant-side regression.
  </action>
  <verify>
    <automated>php artisan test --filter='RolePermissionsTest|BotBrainInvariantTest' --stop-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter='RolePermissionsTest|BotBrainInvariantTest'` exits 0
    - `grep -c "private const MIN_ROLE" app/Auth/RolePermissions.php` equals 1
    - `grep -E "Ability::[A-Za-z]+->value\\s*=>\\s*Role::[A-Za-z]+" app/Auth/RolePermissions.php | wc -l` equals 13 (all abilities mapped)
    - `grep -c "final class RolePermissions" app/Auth/RolePermissions.php` equals 1
    - `./vendor/bin/phpstan analyse app/Auth/RolePermissions.php --no-progress` reports 0 errors
    - BotBrainInvariantTest passes (no Client FormRequest references `bot_custom_instructions`)
  </acceptance_criteria>
  <done>RolePermissions ships with MIN_ROLE mapping all 13 abilities, RolePermissionsTest passes the trivial assertions (52-cell matrix is unskipped in Plan 07 once User::hasRole exists), BotBrainInvariantTest enforces D-09 invariant, PHPStan reports 0 errors.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold all Wave 0 test stub files + SeedsRoleMatrix trait</name>
  <files>
    tests/Concerns/SeedsRoleMatrix.php,
    tests/Unit/Models/UserRoleTest.php,
    tests/Unit/Models/UserRoleResolutionTest.php,
    tests/Feature/Auth/GateRegistrationTest.php,
    tests/Feature/Auth/SingleGuardTest.php,
    tests/Feature/Auth/LoginRedirectTest.php,
    tests/Feature/Auth/RegisterControllerTest.php,
    tests/Feature/Auth/RoleMatrixTest.php,
    tests/Feature/Auth/ChooseRoleTest.php,
    tests/Feature/Admin/RouteGuardTest.php,
    tests/Feature/Client/AbilityCoverageTest.php,
    tests/Feature/Inertia/AuthShareTest.php,
    tests/Feature/Policies/ConversationPolicyTest.php,
    tests/Feature/Policies/LeadPolicyTest.php,
    tests/Feature/Policies/KnowledgeItemPolicyTest.php,
    tests/Feature/Policies/TransactionPolicyTest.php,
    tests/Feature/Webhook/StripeAnonymousTest.php
  </files>
  <read_first>
    - tests/TestCase.php (existing actingAsTenantUser() + createTenantWithUser() pattern, lines 25-57)
    - .planning/phases/16.1-role-foundation/16.1-PATTERNS.md §"tests/Concerns/SeedsRoleMatrix.php"
    - .planning/phases/16.1-role-foundation/16.1-VALIDATION.md §"Wave 0 Requirements"
    - .planning/phases/16.1-role-foundation/16.1-RESEARCH.md §"Wave 0 Gaps" (test file purposes)
  </read_first>
  <behavior>
    - Every Wave 0 test file in the files list exists with a namespace, class skeleton extending TestCase or PHPUnit\Framework\TestCase, and at least one Pest `it(...)` block or PHPUnit `test_...` method
    - Tests that depend on artifacts not yet built (User::hasRole, UserRole pivot table, Gate registration, etc.) use `$this->markTestSkipped('Awaiting Plan NN')` so they parse, autoload, and remain visible in `php artisan test --list-tests` without failing
    - `tests/Concerns/SeedsRoleMatrix.php` exposes `actingAsSuperAdmin()`, `actingAsOwner(Tenant $t)`, `actingAsManager(Tenant $t)`, `actingAsAgent(Tenant $t)` — each returns `static` for fluent chaining (mirroring `actingAsTenantUser()` shape in TestCase.php lines 51-57)
    - All trait helper methods are skipped/no-op until Plan 02 ships UserRole — for now they `throw new \RuntimeException('SeedsRoleMatrix awaiting Plan 02: UserRole model')` so any consumer who runs them prematurely fails loudly
  </behavior>
  <action>
    Create the `tests/Concerns/SeedsRoleMatrix.php` trait per PATTERNS.md "tests/Concerns/SeedsRoleMatrix.php". Mirror the existing fluent-`static`-return pattern from TestCase::actingAsTenantUser(). Each method (`actingAsSuperAdmin`, `actingAsOwner`, `actingAsManager`, `actingAsAgent`) initially throws `RuntimeException('SeedsRoleMatrix: awaiting Plan 02 UserRole + Plan 01 enums')`. Plan 02 Task 4 wires the bodies after UserRole + User::hasRole exist.

    For each test stub file in `<files>`: create the file with `<?php declare(strict_types=1); namespace Tests\<subdir>;` followed by a class skeleton extending `Tests\TestCase` (or `Pest`-style top-level `it(...)` blocks — whichever matches the project's existing test style; check `tests/Feature/` for the dominant pattern). Include exactly one stub assertion (`$this->markTestSkipped('Awaiting Plan NN — see 16.1-VALIDATION.md task ID 16.1-NN-NN')`) that names the plan that will fill the test.

    File-to-plan assignments per VALIDATION matrix:
    - tests/Unit/Models/UserRoleTest.php → Plan 02 (D-03)
    - tests/Unit/Models/UserRoleResolutionTest.php → Plan 02 (D-04)
    - tests/Feature/Auth/GateRegistrationTest.php → Plan 04 (D-10)
    - tests/Feature/Auth/SingleGuardTest.php → Plan 03 (D-05)
    - tests/Feature/Auth/LoginRedirectTest.php → Plan 03 (D-08)
    - tests/Feature/Auth/RegisterControllerTest.php → Plan 05 (D-18)
    - tests/Feature/Auth/RoleMatrixTest.php → Plan 07 (D-19, the 52-cell load-bearing test)
    - tests/Feature/Auth/ChooseRoleTest.php → Plan 03 (D-08 chooser branch)
    - tests/Feature/Admin/RouteGuardTest.php → Plan 03 (D-05 route gating)
    - tests/Feature/Client/AbilityCoverageTest.php → Plan 05 (D-10 controller sweep)
    - tests/Feature/Inertia/AuthShareTest.php → Plan 06a (D-13)
    - tests/Feature/Policies/{Conversation|Lead|KnowledgeItem|Transaction}PolicyTest.php → Plan 04 (D-11)
    - tests/Feature/Webhook/StripeAnonymousTest.php → Plan 03 (Pitfall 7 + threat: webhooks must stay outside RequireSuperAdmin)

    Verify each stub file parses by running `php artisan test --list-tests | grep -c <ClassName>`. The Pest preset auto-discovers files under `tests/Feature` + `tests/Unit` — no `phpunit.xml` edit needed.
  </action>
  <verify>
    <automated>php artisan test --list-tests 2>&1 | grep -cE 'UserRoleTest|UserRoleResolutionTest|GateRegistrationTest|SingleGuardTest|LoginRedirectTest|RegisterControllerTest|RoleMatrixTest|ChooseRoleTest|RouteGuardTest|AbilityCoverageTest|AuthShareTest|ConversationPolicyTest|LeadPolicyTest|KnowledgeItemPolicyTest|TransactionPolicyTest|StripeAnonymousTest'</automated>
  </verify>
  <acceptance_criteria>
    - All 17 listed test stub files exist (16 test files + the SeedsRoleMatrix trait)
    - `php artisan test --list-tests 2>&1 | grep -c 'Tests\\\\'` includes at least 16 new entries
    - `php artisan test 2>&1 | grep -E "PASS|SKIP|Tests:"` does not show ANY new FAILED entries from these stubs (they all skip cleanly)
    - `grep -l "markTestSkipped" tests/Feature/Auth/ tests/Unit/ tests/Feature/Policies/ -r | wc -l` is ≥ 16
    - `grep -c "trait SeedsRoleMatrix" tests/Concerns/SeedsRoleMatrix.php` equals 1
    - `grep -c "actingAsSuperAdmin\\|actingAsOwner\\|actingAsManager\\|actingAsAgent" tests/Concerns/SeedsRoleMatrix.php` equals 4
    - `./vendor/bin/pint --test tests/Concerns/ tests/Unit/Enums/ tests/Unit/Auth/ tests/Unit/Models/ tests/Feature/Auth/ tests/Feature/Inertia/ tests/Feature/Policies/ tests/Feature/Webhook/ tests/Feature/Admin/RouteGuardTest.php tests/Feature/Client/AbilityCoverageTest.php` reports clean
  </acceptance_criteria>
  <done>All Wave 0 stubs exist and are visible to `php artisan test --list-tests`; the full suite runs with 0 new failures (only new skips); Pint clean across the new files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → web app | Authenticated session crosses here; Role/Ability enums + RolePermissions are the static decision basis for everything downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.1.01-01 | Tampering | App\Enums\Ability | mitigate | Backed string enum; invalid values rejected at PHP boundary; per D-21 PHPStan baseline=0 catches typo'd `Ability::Xxx` at static-analysis time |
| T-16.1.01-02 | Elevation of Privilege | App\Auth\RolePermissions::MIN_ROLE | mitigate | `final class` (no subclass override); `private const` map (no runtime mutation); explicit per-ability minimum so SuperAdmin has NO grants-all path (Anti-Pattern: `Gate::before()` rejected in research) |
| T-16.1.01-03 | Information Disclosure | bot_custom_instructions (D-09 invariant) | mitigate | BotBrainInvariantTest greps every Client FormRequest and asserts none reference the field; test fails on any future regression that adds tenant-side validation for the bot brain |
| T-16.1.01-04 | Tampering | Wave 0 test stubs | accept | Stubs ship as `markTestSkipped` until later plans wire them; risk = a test stays skipped past its owning plan. Mitigation deferred to Plan 07's full-matrix verification step which fails if any Wave 0 stub is still skipped at phase end |
</threat_model>

<verification>
Run full suite + scoped checks:

```bash
php artisan test --filter='RoleTest|AbilityTest|RolePermissionsTest|BotBrainInvariantTest' --stop-on-failure
php artisan test                                  # full suite must still pass (no new failures from stubs)
./vendor/bin/phpstan analyse --no-progress        # baseline must stay at 0
./vendor/bin/pint --test                          # style clean across touched files
```

Verify enum exhaustiveness:

```bash
grep -E "case [A-Z][A-Za-z]+\\s*=" app/Enums/Ability.php | wc -l   # must equal 13
grep -E "Ability::[A-Za-z]+->value\\s*=>\\s*Role::[A-Za-z]+" app/Auth/RolePermissions.php | wc -l   # must equal 13
```
</verification>

<success_criteria>
- All 3 production files exist with correct namespace + strict_types
- All 19 Wave 0 test/trait files exist and are visible to the test runner
- RoleTest + AbilityTest + RolePermissionsTest + BotBrainInvariantTest pass
- Full suite still green (no new failures introduced by stubs)
- PHPStan baseline = 0 maintained across the new files
- Pint clean across all touched files
</success_criteria>

<output>
Create `.planning/phases/16.1-role-foundation/16.1-01-SUMMARY.md` when done. Include:
- Tasks completed
- File counts (3 prod + 19 test/trait = 22 new files)
- Test results
- PHPStan + Pint status
- Notes for Plan 02 (especially: User::hasRole and User::hasRoleAtLeast signatures the RolePermissions class targets)
</output>
