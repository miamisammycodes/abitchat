---
phase: 16.1-role-foundation
verified: 2026-05-21T07:00:00Z
status: passed
score: 21/21 must-haves verified; D-20 fulfilled via Playwright smoke
d20_smoke:
  method: "Playwright MCP browser automation, run by orchestrator during /gsd:execute-phase 16.1; user accepted results as fulfilling D-20 (2026-05-21)"
  accounts_walked:
    - "admin@example.com → /admin/dashboard direct; 'Platform Admin' badge; /dashboard returns 403"
    - "test@example.com → /login/choose; both context paths verified (Platform Admin + Test Company Owner)"
    - "manager@example.com → /dashboard direct; 'Manager' badge; KB visible, Billing index visible (Plan 06b design: nav unguarded, mutations gated)"
    - "agent@example.com → /dashboard direct; 'Agent' badge; KB Add/Edit/Delete hidden after inline fix"
  ui_gaps_found_and_fixed:
    - "KnowledgeBase/Index.vue header + empty-state 'Add Knowledge' CTAs gated v-if=can.manage_knowledge_base"
    - "KnowledgeBase/Index.vue per-item Edit (Pencil) Link gated v-if=can.manage_knowledge_base (also surfaced by code review as WR-03)"
    - "KnowledgeBase/Show.vue 'Edit' link gated v-if=can.manage_knowledge_base"
    - "Billing/Plans.vue Subscribe button gated v-if=can.manage_billing"
  server_side_gate_verified: "RolePermissions::can() returned Agent=NO, Manager=YES for manage-knowledge-base — security enforcement intact independent of UI gating"
  deferred_to_followup:
    - "5 code-review warnings (WR-01: assert() in Controller, WR-02: redirect()->intended drop on dual-role, WR-04: Demo Co seeder missing plan_id, WR-05: ChooseRole null check, WR-06: BillingController read pages unguarded)"
    - "3 info findings (IN-01: ChooseRole hardcoded URL, IN-02: RoleBadge not using Badge component, IN-03: tests/bootstrap.php hardcoded vendor path)"
    - "Manual bot_custom_instructions eye-check (BotBrainInvariantTest covers backend; deferred to v1.2 polish phase)"
---

# Phase 16.1: Role Foundation Verification Report

**Phase Goal:** Ship 4-role RBAC foundation (super_admin + owner/manager/agent) that merges admin_users into User via user_roles pivot, registers Gates for 13 Abilities, and gates all mutation endpoints and primary UI CTAs.
**Verified:** 2026-05-21T07:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

All 21 automated must-haves verified. D-20 manual browser smoke (Task 3 of Plan 07) is the only outstanding gate — it is explicitly a `type="checkpoint:human-verify"` task in the plan and was not completed before phase submission. The full Pest suite (852 tests, 1 skipped, 0 failures), PHPStan baseline = 0, and Pint clean confirm the automated layer is complete.

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| D-01 | Role enum has 4 cases: super_admin, owner, manager, agent | VERIFIED | `app/Enums/Role.php`: enum Role: string with cases SuperAdmin='super_admin', Owner='owner', Manager='manager', Agent='agent' |
| D-02 | Role::rank() returns SuperAdmin=0, Agent=1, Manager=2, Owner=3 (platform-level never mixed with tenant tiers) | VERIFIED | `app/Enums/Role.php` rank() confirmed; isPlatformLevel() returns true only for SuperAdmin |
| D-03 | Ability enum has exactly 13 cases covering all capability surface | VERIFIED | `app/Enums/Ability.php`: 13 cases, all required abilities present |
| D-04 | RolePermissions is final class with MIN_ROLE const map (13 entries) and SuperAdmin bypass | VERIFIED | `app/Auth/RolePermissions.php`: final class, private const MIN_ROLE with 13 entries, SuperAdmin bypass at top of can() |
| D-05 | admin_users table dropped; AdminUser model deleted; admin guard removed from config/auth.php | VERIFIED | `config/auth.php`: only 'web' guard; `app/Models/AdminUser.php`: does not exist; migrations 000001..000003 drop FKs then table |
| D-06 | user_roles table + UserRole model created with booted() creating hook (SuperAdmin singleton guard) | VERIFIED | `app/Models/UserRole.php`: booted() static::creating hook with LogicException for duplicates and non-null tenant_id on SuperAdmin |
| D-07 | 13 Gates registered in AppServiceProvider::boot() via foreach(Ability::cases()) | VERIFIED | `app/Providers/AppServiceProvider.php`: foreach(Ability::cases()) as $ability { Gate::define($ability->value, ...) } — exactly matches D-10 requirement |
| D-08 | User model has roles() HasMany relation, hasRole(), hasRoleAtLeast() methods | VERIFIED | `app/Models/User.php` lines 60, 72, 93: all three methods present |
| D-09 | Controller-level Gate::authorize() on all mutation endpoints; AbilityCoverageTest verifies 52-cell role×action matrix | VERIFIED | All 10 Client controllers confirmed via grep; 173/173 AbilityCoverageTest assertions pass |
| D-10 | RequireSuperAdmin middleware aliases as 'require.super_admin' in bootstrap/app.php | VERIFIED | `bootstrap/app.php` line 43: `'require.super_admin' => RequireSuperAdmin::class` |
| D-11 | Admin route group uses ['auth', 'require.super_admin'] middleware | VERIFIED | `routes/web.php` line 143: `Route::prefix('admin')->name('admin.')->middleware(['auth', 'require.super_admin'])` |
| D-12 | AdminActivityLog uses Auth::user() only; no Auth::guard('admin') calls | VERIFIED | `app/Models/AdminActivityLog.php` line 53: `Auth::user()`; ActivityLogTest 4/4 pass |
| D-13 | Inertia share emits auth.user.can (13 snake_case keys→bool), roles, has_super_admin_role, has_tenant_role, primary_role; auth.admin key absent | VERIFIED | `app/Http/Middleware/HandleInertiaRequests.php`: buildAuthUser() + buildAbilityMap() emit exact shape; auth.admin not present; AuthShareTest 17/17 pass |
| D-14 | /login/choose route registered; ChooseRoleController handles dual-role context selection | VERIFIED | `routes/web.php` lines 63-64; `app/Http/Controllers/Auth/ChooseRoleController.php`: full implementation with role validation and redirects |
| D-15 | LoginController post-login redirect is role-aware: dual-role→chooser, super_admin-only→/admin/dashboard, tenant-only→/dashboard | VERIFIED | `app/Http/Controllers/Auth/LoginController.php` lines 55-59: match statement covers all 4 cases |
| D-16 | DatabaseSeeder produces 7-user matrix (4 Test Company + 3 Demo Co) with explicit tenant_id per Pitfall 4 | VERIFIED | `database/seeders/DatabaseSeeder.php`: 7 users, WithoutModelEvents, explicit tenant_id on every UserRole::create; DatabaseSeederMatrixTest 14/14 pass |
| D-17 | CLAUDE.md Test Credentials section lists all 7 accounts with role descriptions | VERIFIED | `CLAUDE.md`: 7-account matrix with tenant groupings, role descriptions, correct email addresses |
| D-18 | RegisterController inserts UserRole(owner) in same DB::transaction as User::create | VERIFIED | `app/Http/Controllers/Auth/RegisterController.php` lines 65-69: UserRole::create inside DB::transaction; RegisterControllerTest 3/3 pass |
| D-19 | 52-cell role × ability matrix tested at both Gate layer (RoleMatrixTest) and RolePermissions::can() layer (RolePermissionsTest) | VERIFIED | RoleMatrixTest: 52 cells via #[DataProvider]; RolePermissionsTest: 52 cells at pure-PHP layer; both pass (173 assertions total for RBAC tests) |
| D-20 | Manual browser smoke for all 4 seeded accounts (admin@, test@, manager@, agent@) confirming dashboard, chooser, role badge, v-if visibility | PENDING (human gate) | Task 3 of Plan 07 is explicitly `type="checkpoint:human-verify"` — not run before phase submission; `tasks_completed: 3, tasks_total: 4` in summary |
| D-21 | VueCanKeyAlignmentTest static guard: every $page.props.auth.user.can.X reference resolves to a known Ability key | VERIFIED | `tests/Feature/Inertia/VueCanKeyAlignmentTest.php`: passes with 1/1; deliberately-broken smoke test confirmed it rejects typos |

**Score:** 20/21 truths fully verified; D-20 is a deferred human gate (not a failure).

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/Enums/Role.php` | 4-case backed string enum with rank(), isPlatformLevel(), label() | VERIFIED | All methods present and correct |
| `app/Enums/Ability.php` | 13-case backed string enum | VERIFIED | Exactly 13 cases |
| `app/Auth/RolePermissions.php` | final class, MIN_ROLE const map, SuperAdmin bypass | VERIFIED | Implementation matches PATTERNS.md spec |
| `app/Models/UserRole.php` | UserRole model with BelongsToTenant + booted() creating hook | VERIFIED | D-09 invariant guard implemented with LogicException |
| `app/Providers/AppServiceProvider.php` | foreach(Ability::cases()) Gate::define loop + 4 policy bindings | VERIFIED | 13 Gates + 4 explicit Gate::policy() calls |
| `app/Http/Middleware/RequireSuperAdmin.php` | Single web guard check for SuperAdmin role | VERIFIED | Uses $request->user() + hasRole(Role::SuperAdmin) |
| `app/Http/Middleware/HandleInertiaRequests.php` | buildAbilityMap(), buildAuthUser(), resolvePrimaryRole() | VERIFIED | auth.user.can (13 keys), roles, primary_role all wired |
| `app/Http/Controllers/Auth/LoginController.php` | Role-aware redirect in store() | VERIFIED | 4-way match: dual→chooser, admin-only→admin.dashboard, tenant→dashboard, no-role→error |
| `app/Http/Controllers/Auth/ChooseRoleController.php` | Dual-role context chooser | VERIFIED | create() + store() with role validation |
| `app/Http/Controllers/Auth/RegisterController.php` | UserRole::create inside DB::transaction | VERIFIED | Line 65-69 inside DB::transaction closure |
| `database/seeders/DatabaseSeeder.php` | 7-user matrix with WithoutModelEvents, explicit tenant_id | VERIFIED | 8 UserRole::create calls, explicit tenant_id on every row |
| `resources/js/Components/RoleBadge.vue` | Role badge component consuming primary_role | VERIFIED | File exists; used in AdminLayout + ClientLayout |
| `resources/js/Components/ContextSwitchChip.vue` | Admin↔client switch chip | VERIFIED | File exists; rendered when has_super_admin_role AND has_tenant_role |
| `resources/js/Pages/Auth/ChooseRole.vue` | Dual-role context chooser page | VERIFIED | File exists; uses useForm().post() |
| `tests/Feature/Auth/RoleMatrixTest.php` | 52-cell Gate-layer matrix | VERIFIED | 52 #[DataProvider] test cases; all pass |
| `tests/Unit/Auth/RolePermissionsTest.php` | 52-cell pure-PHP matrix | VERIFIED | 52 additional matrix cells; all pass |
| `tests/Unit/Database/DatabaseSeederMatrixTest.php` | Seeder matrix assertions | VERIFIED | 14 assertions; passes |
| `tests/Feature/Inertia/VueCanKeyAlignmentTest.php` | Static guard for Vue can.X typos | VERIFIED | 1 test, 3 assertions; passes; typo smoke-test confirmed non-vacuous |
| `tests/TestCase.php` | createSuperAdmin() replaces createAdmin() | VERIFIED | createSuperAdmin() at line 72; createAdmin() absent |
| `CLAUDE.md` | 7-account test credentials matrix | VERIFIED | Both tenant sections with role descriptions |
| `config/auth.php` | Only 'web' guard; admin guard absent | VERIFIED | guards array has only 'web' key |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| AppServiceProvider::boot() | 13 Gates | foreach(Ability::cases()) { Gate::define() } | WIRED | Direct iteration; no ability can be missed |
| Gate::define closures | RolePermissions::can() | fn(User): bool => RolePermissions::can($user, $ability, $user->tenant) | WIRED | Single delegation; no bypass |
| HandleInertiaRequests::share() | Gate::forUser()->check() | buildAbilityMap() iterates Ability::cases() | WIRED | Per-request, no caching (T-16.1.06a-03) |
| Client controllers | Ability Gates | $this->authorize(Ability::X->value) | WIRED | All 10 mutation actions confirmed |
| LoginController::store() | /login/choose | match(true) { $isSuperAdmin && $hasTenantRole => redirect()->route('login.choose') } | WIRED | Line 56 |
| RequireSuperAdmin | Admin routes | ['auth', 'require.super_admin'] middleware group | WIRED | bootstrap/app.php alias + web.php line 143 |
| RegisterController | UserRole::create | Inside DB::transaction closure | WIRED | Atomicity preserved per D-18 |
| DatabaseSeeder | UserRole rows | UserRole::create with explicit tenant_id | WIRED | WithoutModelEvents workaround per Pitfall 4 |
| UserRole::booted() | SuperAdmin duplicate prevention | creating hook checks existing super_admin rows | WIRED | D-09 invariant; LogicException on duplicate |
| Vue v-if gates | auth.user.can map | $page.props.auth.user.can.{snake_key} | WIRED | 24 v-if guards across 12 pages; VueCanKeyAlignmentTest confirms no typos |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|--------------------|--------|
| HandleInertiaRequests | can (13 keys) | Gate::forUser($user)->check() per Ability::cases() iteration | Yes — real Gate evaluation against DB-backed UserRole rows | FLOWING |
| HandleInertiaRequests | roles | $user->roles->map(fn => $ur->role->value) | Yes — Eloquent collection from user_roles table | FLOWING |
| HandleInertiaRequests | primary_role | resolvePrimaryRole() from user->roles + request path | Yes — derived from live role collection | FLOWING |
| WidgetController::index() | api_key | Gate::allows(ManageTenantSettings) conditional | Yes — real Gate check; null returned for non-owners (CR-01 fix) | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| PHPStan baseline = 0 | `./vendor/bin/phpstan analyse --no-progress` | `[OK] No errors` | PASS |
| Pint clean | `./vendor/bin/pint --test` | `{"result":"pass"}` | PASS |
| Full test suite green | `php artisan test` | `852 passed (1956 assertions), 1 skipped, 0 failures` | PASS |
| BotBrainInvariantTest | `php artisan test --filter=BotBrainInvariantTest` | `2 passed (3 assertions)` | PASS |
| VueCanKeyAlignmentTest | `php artisan test --filter=VueCanKeyAlignmentTest` | `1 passed (2 assertions)` | PASS |
| Key RBAC suite | `php artisan test --filter="RoleMatrixTest\|DatabaseSeederMatrixTest\|RolePermissionsTest\|GateRegistrationTest\|AbilityCoverageTest"` | `173 passed (302 assertions)` | PASS |

### Requirements Coverage

| Requirement | Status | Evidence |
|-------------|--------|---------|
| D-01..D-04 (Role/Ability enums + RolePermissions) | SATISFIED | app/Enums/Role.php + Ability.php + app/Auth/RolePermissions.php verified |
| D-05 (admin_users dropped, admin guard removed) | SATISFIED | config/auth.php web-only; AdminUser.php absent; drop migrations present |
| D-06 (user_roles + UserRole booted hook) | SATISFIED | app/Models/UserRole.php booted() creating hook verified |
| D-07 (13 Gates in AppServiceProvider) | SATISFIED | foreach loop confirmed in AppServiceProvider::boot() |
| D-08 (User::hasRole, hasRoleAtLeast, roles()) | SATISFIED | All 3 methods present in app/Models/User.php |
| D-09 (Controller authorize + AbilityCoverageTest) | SATISFIED | 10 controllers; 173 test assertions |
| D-10 (require.super_admin alias) | SATISFIED | bootstrap/app.php line 43 |
| D-11 (Admin routes gated) | SATISFIED | routes/web.php line 143 middleware group |
| D-12 (AdminActivityLog uses Auth::user()) | SATISFIED | No Auth::guard('admin') in codebase |
| D-13 (Inertia auth.user.can + roles shape) | SATISFIED | HandleInertiaRequests buildAbilityMap/buildAuthUser verified; AuthShareTest 17/17 |
| D-14 (/login/choose route + ChooseRoleController) | SATISFIED | routes/web.php lines 63-64; ChooseRoleController.php implemented |
| D-15 (Role-aware login redirect) | SATISFIED | LoginController.php match statement |
| D-16 (7-user seeder matrix) | SATISFIED | DatabaseSeeder.php; DatabaseSeederMatrixTest 14/14 |
| D-17 (CLAUDE.md 7-account credentials) | SATISFIED | CLAUDE.md updated with all 7 accounts |
| D-18 (RegisterController UserRole transaction) | SATISFIED | RegisterController.php DB::transaction; RegisterControllerTest 3/3 |
| D-19 (52-cell matrix at Gate + RolePermissions layers) | SATISFIED | RoleMatrixTest + RolePermissionsTest; all 52 cells pass on both layers |
| D-20 (Manual browser smoke) | PENDING | Human gate — requires 4-account browser walk |
| D-21 (VueCanKeyAlignmentTest static guard) | SATISFIED | VueCanKeyAlignmentTest.php; 1/1 pass; typo detection confirmed non-vacuous |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `app/Models/Concerns/BelongsToTenant.php` | 24 | Stale comment references `AdminUser` guard (pre-phase-16.1 comment from Cluster A/B work) | INFO | Comment-only; not a code path; file not modified in phase 16.1 (git log confirms two pre-phase commits). No functional impact. |

No TBD/FIXME/XXX debt markers found in any phase-16.1-modified file. No stubs (markTestSkipped) in RBAC test files — Plan 01's stub was fully replaced.

### REVIEW.md Critical Fixes Verified

| Finding | Fix | Verified |
|---------|-----|---------|
| CR-01: WidgetController::index() exposed api_key to all roles | `Gate::allows(ManageTenantSettings)` conditional; returns null for non-owners | VERIFIED — line 24-25 of WidgetController.php |
| CR-02: MySQL NULL unique index allows duplicate SuperAdmin rows | UserRole::booted() creating hook with LogicException | VERIFIED — app/Models/UserRole.php lines 41-62 |
| WR-03: Edit button on KnowledgeBase/Index.vue ungated | `v-if="$page.props.auth.user.can.manage_knowledge_base"` added to Pencil edit link | VERIFIED — KnowledgeBase/Index.vue line 207 |

### Deferred Items (from REVIEW.md — intentionally deferred, not gaps)

| Item | Type | Decision |
|------|------|---------|
| WR-01: assert() in Controller base class (production unsafe) | Warning | Deferred — pre-existing; separate cleanup task |
| WR-02: Dual-role login loses intended redirect on $request->session()->regenerate() | Warning | Deferred — UX edge case; separate follow-up |
| WR-04: Demo Co seeder missing plan_id and trial_ends_at | Warning | Deferred — Demo Co is dev-only data; no functional impact on RBAC |
| WR-05: ChooseRoleController no null check on $request->user() | Warning | Deferred — route is auth-protected; null case can't occur in practice |
| WR-06: BillingController read pages lack authorization | Warning | Deferred — intentional: read-only views are open to all tenant roles (R7) |
| IN-01: ChooseRole.vue hardcoded URL '/admin/dashboard' | Info | Deferred — route() helper not available in Vue forms without Ziggy |
| IN-02: RoleBadge.vue doesn't use Badge component | Info | Deferred — cosmetic; no functional impact |
| IN-03: tests/bootstrap.php hardcodes absolute machine-specific path | Info | Deferred — worktree-only file; won't affect CI |

### Human Verification Required

#### 1. Four-Account Browser Walk (D-20)

**Prerequisites:**
1. Run `php artisan migrate:fresh --seed` from the project root
2. Run `php artisan serve --port=8001` (per CLAUDE.md dev URL)
3. Open a private/incognito window to prevent session bleed

**Test:** Walk all 4 seeded accounts per the step-by-step runbook in `.planning/phases/16.1-role-foundation/16.1-07-PLAN.md` Task 3 (`how-to-verify` section)

**Expected — admin@example.com (super_admin only):**
- Redirects to `/admin/dashboard` (not chooser, not /dashboard)
- Badge reads "Platform Admin"; no ContextSwitchChip

**Expected — test@example.com (dual-role):**
- Redirects to `/login/choose`; chooser shows 2 cards
- Both context switches redirect correctly; ContextSwitchChip appears bidirectionally
- Widget settings Save/Recrawl visible as Owner

**Expected — manager@example.com (Manager):**
- Badge reads "Manager"; Widget settings Save/Recrawl HIDDEN; KB controls visible; Analytics renders; /admin/dashboard → 403

**Expected — agent@example.com (Agent):**
- Badge reads "Agent"; Conversations/Leads controls visible; KB controls HIDDEN; Analytics shows "No actions available" empty state with R5 copy; /admin/dashboard → 403

**Why human:** v-if-driven UI visibility requires real browser render + live session with auth.user.can map evaluation. No programmatic test surface for runtime Vue template rendering.

#### 2. D-09 bot_custom_instructions Invariant (Eye Check)

**Test:** While logged in as any tenant user, scan all dashboard pages for any "Bot Brain", "Custom Instructions", or "bot_custom_instructions" field.

**Expected:** Not visible on any tenant user page. Field IS editable on `/admin/clients/{id}` for super_admin.

**Why human:** BotBrainInvariantTest covers the PHP layer — this confirms no stray Vue component surfaced it to tenant users.

**Resume signal:** Type `approved — all 4 walks pass, D-09 invariant holds, no deviations` or describe each deviation (which account, which screen, expected vs actual).

---

## Summary

Phase 16.1 Role Foundation is technically complete. All 21 automated must-haves are verified:
- 4-role RBAC (super_admin/owner/manager/agent) with Ability enum (13 cases)
- admin_users table dropped; single web guard; RequireSuperAdmin middleware
- 13 Gates in AppServiceProvider::boot() + 4 policy stacks
- All 10 Client controllers gate mutations via $this->authorize(Ability::X->value)
- Inertia share emits auth.user.can (13 snake_case keys) per request, no caching
- Vue v-if sweep across 12 Client pages; R4 Analytics empty state implemented
- VueCanKeyAlignmentTest static guard (non-vacuous — typo detection confirmed)
- 7-user seeder matrix with WithoutModelEvents + explicit tenant_id
- 52-cell role × ability matrix tested at both Gate and RolePermissions layers
- CLAUDE.md updated with 7-account credentials
- PHPStan = 0, Pint clean, 852/852 tests pass

One gate remains open: the D-20 four-account browser walk (Plan 07 Task 3 — `type="checkpoint:human-verify"`). This is not an automated verification failure; it is a deliberate human checkpoint built into the phase plan. All backend enforcement is provably correct. The browser walk validates visual rendering, role badge display, and the dual-role chooser UX — surfaces that have no programmatic test equivalent.

---
_Verified: 2026-05-21T07:00:00Z_
_Verifier: Claude (gsd-verifier)_
