---
phase: 16.1-role-foundation
plan: 05
type: execute
wave: 5
depends_on:
  - 16.1-04
files_modified:
  - app/Http/Controllers/Client/BillingController.php
  - app/Http/Controllers/Client/DkBankQrController.php
  - app/Http/Controllers/Client/KnowledgeBaseController.php
  - app/Http/Controllers/Client/LeadController.php
  - app/Http/Controllers/Client/ConversationController.php
  - app/Http/Controllers/Client/WebsiteIndexingController.php
  - app/Http/Controllers/Client/WidgetController.php
  - app/Http/Controllers/Client/DashboardController.php
  - app/Http/Controllers/Client/AnalyticsController.php
  - app/Http/Controllers/Client/EnterpriseInquiryController.php
  - app/Http/Controllers/Auth/RegisterController.php
  - app/Models/AdminActivityLog.php
  - tests/Feature/Client/AbilityCoverageTest.php
  - tests/Feature/Auth/RegisterControllerTest.php
  - tests/Feature/Admin/ActivityLogTest.php
autonomous: true
requirements:
  - D-09
  - D-10
  - D-18
  - D-21

must_haves:
  truths:
    - "Every Client controller mutation action calls $this->authorize(Ability::X->value) per the canonical matrix"
    - "DashboardController + AnalyticsController require Ability::ViewDashboard / ViewAnalyticsFull respectively"
    - "BillingController + DkBankQrController write actions require Ability::ManageBilling"
    - "KnowledgeBaseController mutations require Ability::ManageKnowledgeBase"
    - "ConversationController + LeadController mutations require Ability::ManageConversations / ManageLeads"
    - "WebsiteIndexingController + WidgetController mutations (brand settings) require Ability::ManageTenantSettings"
    - "RegisterController::store inserts a UserRole(role=owner, tenant_id=tenant.id) row inside the existing DB transaction; the legacy 'role' => 'owner' in User::create is removed"
    - "AdminActivityLog::log no longer calls Auth::guard('admin')->user(); reads Auth::user() and asserts SuperAdmin role"
    - "grep across app/ returns 0 hits for Auth::guard\\(.admin.\\) and 0 hits for isOwner|isManager|isAgent (when filtered to excluded the deleted helper definitions)"
  artifacts:
    - path: "app/Http/Controllers/Auth/RegisterController.php"
      provides: "Public signup creating User + UserRole(owner) in same transaction"
      contains: "UserRole::create"
    - path: "app/Models/AdminActivityLog.php"
      provides: "::log() reads Auth::user() and enforces SuperAdmin role"
      contains: "$user->hasRole(Role::SuperAdmin)"
  key_links:
    - from: "app/Http/Controllers/Client/BillingController.php"
      to: "Gate (ManageBilling)"
      via: "$this->authorize(Ability::ManageBilling->value)"
      pattern: "authorize\\(Ability::ManageBilling->value"
    - from: "app/Http/Controllers/Auth/RegisterController.php"
      to: "App\\Models\\UserRole"
      via: "UserRole::create inside DB::transaction"
      pattern: "UserRole::create\\("
---

<objective>
Sweep every Client controller to add `$this->authorize(Ability::X->value)` calls per the matrix; fix `RegisterController` to insert the `user_roles` pivot row in the same DB transaction as `User::create`; rewrite `AdminActivityLog::log()` to read `Auth::user()` instead of the deleted `admin` guard. This plan finishes the controller-level enforcement that Plan 04a's Gate registration unlocks.

Purpose: Without this plan, Gates are registered but unused — `$this->authorize()` calls have to exist at every mutation site for the matrix to take effect. RegisterController fix preserves the "first user is owner of their new tenant" guarantee on top of the multi-role pivot. AdminActivityLog::log fix prevents every admin write action from 500-ing once the `admin` guard is gone.

Output: 10 modified controllers + 1 modified model + 3 implemented tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16.1-role-foundation/16.1-CONTEXT.md
@.planning/phases/16.1-role-foundation/16.1-RESEARCH.md
@.planning/phases/16.1-role-foundation/16.1-PATTERNS.md
@.planning/phases/16.1-role-foundation/16.1-VALIDATION.md
@.planning/phases/16.1-role-foundation/16.1-03-SUMMARY.md
@.planning/phases/16.1-role-foundation/16.1-04-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Plan 01..04 already shipped. This plan threads through every controller. -->

From Plan 04 (already shipped) — Gates are registered for these strings:
- 'view-admin-dashboard', 'manage-tenant-as-admin', 'manage-platform-settings'  (SuperAdmin)
- 'manage-billing', 'manage-team', 'manage-tenant-settings', 'delete-tenant'   (Owner)
- 'manage-knowledge-base', 'manage-integrations', 'view-analytics-full'         (Manager+)
- 'manage-conversations', 'manage-leads', 'view-dashboard'                       (Agent+)

<!-- Per-controller ability matrix (derived from CONTEXT.md D-09..D-12): -->

| Controller (Client/) | Mutation actions | Min ability |
|----------------------|------------------|-------------|
| BillingController | upgrade, cancel, reactivate, etc. (every write) | manage-billing (Owner) |
| DkBankQrController | initiate, verifyRrn, etc. (write) | manage-billing (Owner) |
| KnowledgeBaseController | store, update, destroy, refresh, etc. | manage-knowledge-base (Manager+) |
| LeadController | store, update, destroy, export, merge | manage-leads (Agent+) |
| ConversationController | takeover, update, destroy | manage-conversations (Agent+) |
| WebsiteIndexingController | store/update (the original trigger) | manage-tenant-settings (Owner) |
| WidgetController | brand/welcome_message/primary_color/position updates | manage-tenant-settings (Owner) |
| DashboardController | index (read) | view-dashboard (Agent+) — confirm whether this even needs authorize or just `auth` middleware is enough |
| AnalyticsController | index, etc. | view-analytics-full (Manager+) |
| EnterpriseInquiryController | submit (anonymous or auth?) — verify; if auth, gate at view-dashboard |

<!-- Existing $this->authorize() call sites (model-policy, preserved): -->

- BillingController:153, DkBankQrController:41/66/83, KnowledgeBaseController:130/168/183/211/246/264, LeadController:85/99/131/141
- These stay; ADD ability-name authorize calls at action entry.

<!-- RegisterController target shape (PATTERNS.md): -->

```php
$user = User::create([...]);   // 'role' => 'owner' REMOVED
UserRole::create([
    'user_id'   => $user->id,
    'role'      => Role::Owner,
    'tenant_id' => $tenant->id,
]);
```

<!-- AdminActivityLog target shape (PATTERNS.md): -->

```php
public static function log(string $actionType, ?Model $target = null, array $details = []): self {
    $user = Auth::user();
    if (! $user instanceof User || ! $user->hasRole(Role::SuperAdmin)) {
        throw new \LogicException('AdminActivityLog::log called outside super_admin context');
    }
    return self::create(['admin_user_id' => $user->id, ...]);
}
```

The `admin_user_id` column NAME is preserved (Pitfall 3 Option A). Only the FK target and Auth::user() source change.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Client controllers authorize sweep — add $this->authorize(Ability::X->value) per matrix</name>
  <files>app/Http/Controllers/Client/BillingController.php, app/Http/Controllers/Client/DkBankQrController.php, app/Http/Controllers/Client/KnowledgeBaseController.php, app/Http/Controllers/Client/LeadController.php, app/Http/Controllers/Client/ConversationController.php, app/Http/Controllers/Client/WebsiteIndexingController.php, app/Http/Controllers/Client/WidgetController.php, app/Http/Controllers/Client/DashboardController.php, app/Http/Controllers/Client/AnalyticsController.php, app/Http/Controllers/Client/EnterpriseInquiryController.php, tests/Feature/Client/AbilityCoverageTest.php</files>
  <read_first>
    - app/Http/Controllers/Client/LeadController.php (lines 85, 99, 131, 141 — existing $this->authorize('view', $lead) calls; this is the analog for stacking ability-name calls next to model-policy calls)
    - app/Http/Controllers/Client/BillingController.php (line 153 existing authorize call)
    - app/Http/Controllers/Client/DkBankQrController.php (lines 41, 66, 83 existing authorize)
    - app/Http/Controllers/Client/KnowledgeBaseController.php (lines 130, 168, 183, 211, 246, 264 existing authorize)
    - .planning/phases/16.1-role-foundation/16.1-PATTERNS.md §"Authorize Call-Site Pattern (existing, preserved)"
    - .planning/phases/16.1-role-foundation/16.1-CONTEXT.md §"Permission Matrix" (D-09..D-12)
    - .planning/phases/16.1-role-foundation/16.1-02-SUMMARY.md (list of call sites still using deleted User::isOwner/isManager/isAgent — every one of those must be migrated in this task)
  </read_first>
  <behavior>
    - Every Client controller mutation action begins with `$this->authorize(Ability::X->value)` where X is the matrix-mandated ability (see interfaces table above)
    - Existing model-policy `$this->authorize('view', $resource)` calls STAY (per PATTERNS.md "Authorize Call-Site Pattern (existing, preserved)") — they now stack with the upgraded policies from Plan 04
    - Read-only actions on resources with model policies (e.g., `show`, `index` for Lead) inherit their gate from the policy upgrades (Plan 04) — no additional ability-name authorize call needed unless the matrix requires a tier above what the policy provides
    - DashboardController::index calls `$this->authorize(Ability::ViewDashboard->value)` (Agent+) so even a logged-in user with no tenant role (a no-role edge) gets 403 instead of a blank dashboard
    - AnalyticsController calls `$this->authorize(Ability::ViewAnalyticsFull->value)` (Manager+) so Agents cannot read analytics
    - WidgetController write actions (brand settings) call `$this->authorize(Ability::ManageTenantSettings->value)`
    - All deleted-helper call sites (`->isOwner()`, `->isManager()`, `->isAgent()`, `->hasRole(string)`) are removed and replaced with Gate-based checks
  </behavior>
  <action>
    Use the call-site list from Plan 02's SUMMARY as the ground truth: every grep-hit for `->isOwner()`, `->isManager()`, `->isAgent()`, `->hasRole(string)` outside `app/Models/User.php` MUST be migrated.

    For each controller in `<files>`:
    1. Read the file in full.
    2. Identify mutation actions (POST/PUT/PATCH/DELETE handlers — Laravel convention: `store`, `update`, `destroy`, plus any custom mutation like `approve`, `reject`, `merge`, `export`, `restore`, `recrawl`).
    3. At the top of each mutation action body, add `$this->authorize(Ability::X->value);` per the matrix table above. Place it AFTER `$this->authorize('action', $model)` policy calls if both apply (model-policy first, then ability gate — failing closer to the action keeps debug traces clean).
    4. For read-only actions (index, show), authorize only if the matrix requires a tier above the policy (e.g., AnalyticsController::index requires Manager+; DashboardController::index requires Agent+).
    5. Remove every reference to the deleted helpers in this file: `->isOwner()`, `->isManager()`, `->isAgent()`, `->hasRole(string)`. Replace with `Gate::allows(Ability::X->value)` if the call was in a conditional, or migrate the whole conditional to `$this->authorize(Ability::X->value)` if it gated the entire action.

    Add imports as needed: `use App\Enums\Ability;`, `use Illuminate\Support\Facades\Gate;` (the latter only if a `Gate::allows` call replaces a helper inside an `if` body).

    Per CLAUDE.md "Legacy Code Cleanup": NEVER leave a dual-pattern fallback like `$user->isOwner() ?? Gate::allows(...)`. Choose one.

    Implement AbilityCoverageTest (unskip Plan 01 — covers D-10):
    - For each mutation route under `/dashboard/*`, hit the route as:
      * unauthenticated → expect 302 to /login
      * authenticated Owner → expect 200 / 302 (success)
      * authenticated Manager → expect 200 if matrix allows (Manager+ abilities) OR 403 if not (Owner-only)
      * authenticated Agent → expect 200 if matrix allows (Agent+) OR 403 otherwise
      * authenticated tenant B user → expect 403 (cross-tenant)
    - The test data-providers over the route list. Use `php artisan route:list --name=dashboard --json` programmatically — or hard-code the route names if the data-provider approach gets noisy. Either is acceptable; prefer hard-coded for stable failures.
    - Also assert grep guard: `app/Http/Controllers/Client/**/*.php` contains zero matches for `->isOwner()`, `->isManager()`, `->isAgent()`. Implement via `glob + file_get_contents + assertStringNotContainsString` loop.

    The PHPStan baseline=0 gate ensures any forgotten reference to the deleted methods is caught at CI time even if a manual grep misses it.
  </action>
  <verify>
    <automated>php artisan test --filter=AbilityCoverageTest --stop-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "authorize(Ability::ManageBilling->value)" app/Http/Controllers/Client/BillingController.php` ≥ 1
    - `grep -c "authorize(Ability::ManageBilling->value)" app/Http/Controllers/Client/DkBankQrController.php` ≥ 1
    - `grep -c "authorize(Ability::ManageKnowledgeBase->value)" app/Http/Controllers/Client/KnowledgeBaseController.php` ≥ 1
    - `grep -c "authorize(Ability::ManageLeads->value)" app/Http/Controllers/Client/LeadController.php` ≥ 1
    - `grep -c "authorize(Ability::ManageConversations->value)" app/Http/Controllers/Client/ConversationController.php` ≥ 1
    - `grep -c "authorize(Ability::ManageTenantSettings->value)" app/Http/Controllers/Client/WebsiteIndexingController.php` ≥ 1
    - `grep -c "authorize(Ability::ManageTenantSettings->value)" app/Http/Controllers/Client/WidgetController.php` ≥ 1
    - `grep -c "authorize(Ability::ViewAnalyticsFull->value)" app/Http/Controllers/Client/AnalyticsController.php` ≥ 1
    - `grep -rnE "->isOwner\\(\\)|->isManager\\(\\)|->isAgent\\(\\)" app/Http/Controllers/Client/` returns 0 hits
    - `grep -rnE "->hasRole\\('(owner|manager|agent|super_admin)'\\)" app/Http/Controllers/Client/` returns 0 hits (the old string-arg hasRole helper is gone)
    - AbilityCoverageTest exits 0
    - `./vendor/bin/phpstan analyse app/Http/Controllers/Client/ --no-progress` reports 0 errors
  </acceptance_criteria>
  <done>All 10 Client controllers carry the matrix-mandated authorize calls; AbilityCoverageTest exercises the full role × action grid; legacy helper references removed; PHPStan baseline=0 maintained.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RegisterController — insert UserRole(role=owner) inside the existing DB transaction</name>
  <files>app/Http/Controllers/Auth/RegisterController.php, tests/Feature/Auth/RegisterControllerTest.php</files>
  <read_first>
    - app/Http/Controllers/Auth/RegisterController.php (entire file — pay attention to the `DB::transaction(function () use ($request) {...})` block; the `User::create(['role' => 'owner', ...])` is what changes)
    - .planning/phases/16.1-role-foundation/16.1-PATTERNS.md §"app/Http/Controllers/Auth/RegisterController.php"
    - .planning/phases/16.1-role-foundation/16.1-RESEARCH.md §"Pitfall 4" (BelongsToTenant + WithoutModelEvents — applies to seeder, NOT to this controller; but explicitly pass tenant_id for consistency)
  </read_first>
  <behavior>
    - User::create no longer includes `'role' => 'owner'` (column dropped by Plan 02)
    - Immediately after User::create, UserRole::create inserts a row with user_id=$user->id, role=Role::Owner, tenant_id=$tenant->id — all within the same DB::transaction closure that wraps Tenant::create + User::create
    - If UserRole::create throws, the entire transaction rolls back (Tenant + User both gone) — verifiable by forcing an exception (e.g., disable the user_roles table for the test, expect rollback)
    - The Auth::login($user) + event(new Registered($user)) calls remain unchanged at the end of the transaction
    - 1 SuperAdmin signup path: NONE — per D-18, public registration only creates owners. No matrix change.
  </behavior>
  <action>
    Modify `app/Http/Controllers/Auth/RegisterController.php`:
    1. Remove `'role' => 'owner'` from the `User::create([...])` call inside the DB::transaction closure. Per Plan 02 the column is dropped.
    2. Immediately after `User::create([...])`, insert the `UserRole::create([...])` call per the verbatim excerpt in PATTERNS.md §"app/Http/Controllers/Auth/RegisterController.php". The pivot row's keys: `user_id` = `$user->id`, `role` = `Role::Owner`, `tenant_id` = `$tenant->id`. All three keys MUST be explicit (Pitfall 4 — tenant_id is hardcoded server-side, never accepted from the request body).

    3. Add imports: `use App\Enums\Role;`, `use App\Models\UserRole;`.
    4. Per CLAUDE.md debug logging, optionally add `Log::debug('[Auth] (NO $) Registered owner role', ['user_id' => $user->id, 'tenant_id' => $tenant->id])` AFTER the UserRole::create.

    The DB::transaction wrapping is already in place (lines 37-68 per PATTERNS.md analog). Do NOT introduce a second transaction — reuse the existing closure.

    Implement RegisterControllerTest (unskip Plan 01 — covers D-18):
    - Happy path: POST /register with valid signup form → expect 302 to /dashboard. Assert a new `users` row exists, a new `tenants` row exists, AND a `user_roles` row with role=owner + tenant_id=new_tenant_id exists. Assert Auth::user() returns the new user.
    - Transactional rollback: mock UserRole::create to throw (or pre-create a unique constraint conflict via `User::create` with the same email and explicit user_id collision). Assert that AFTER the failed POST, NO Tenant + NO User + NO UserRole row exists in the DB (rollback honored).
    - No role mass-assignment: POST /register with a body that includes `role=super_admin` → expect 302 + a UserRole row with role=owner (not super_admin). This guards against tampering attacks that exploit mass-assignment.
  </action>
  <verify>
    <automated>php artisan test --filter=RegisterControllerTest --stop-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'role' => 'owner'" app/Http/Controllers/Auth/RegisterController.php` equals 0 (legacy column assignment removed)
    - `grep -c "UserRole::create" app/Http/Controllers/Auth/RegisterController.php` equals 1
    - `grep -c "Role::Owner" app/Http/Controllers/Auth/RegisterController.php` equals 1
    - `grep -c "DB::transaction" app/Http/Controllers/Auth/RegisterController.php` ≥ 1 (the existing closure is preserved)
    - RegisterControllerTest exits 0 (happy-path + rollback + mass-assignment-guard all pass)
    - `./vendor/bin/phpstan analyse app/Http/Controllers/Auth/RegisterController.php --no-progress` reports 0 errors
  </acceptance_criteria>
  <done>RegisterController creates User + UserRole(owner) in one transaction; mass-assignment of role rejected; rollback verified; D-18 satisfied.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AdminActivityLog::log() rewrite — read Auth::user(), assert SuperAdmin role</name>
  <files>app/Models/AdminActivityLog.php, tests/Feature/Admin/ActivityLogTest.php</files>
  <read_first>
    - app/Models/AdminActivityLog.php (current state — find the `::log()` static method calling `Auth::guard('admin')->user()`)
    - .planning/phases/16.1-role-foundation/16.1-PATTERNS.md §"app/Models/AdminActivityLog.php"
    - .planning/phases/16.1-role-foundation/16.1-RESEARCH.md §"Pitfall 2: AdminActivityLog::log() reads Auth::guard('admin')"
  </read_first>
  <behavior>
    - AdminActivityLog::log($actionType, ?$target, $details) reads `Auth::user()` (the unified web guard)
    - If the resolved user is null OR not a User instance OR does not hold Role::SuperAdmin, the method throws \LogicException with message 'AdminActivityLog::log called outside super_admin context'
    - On the happy path, the row inserted has admin_user_id = $user->id (column name preserved per Pitfall 3 Option A) and the existing action_type/details/target_type/target_id columns populated as before
    - Any existing PHPDoc relation method (`admin()` BelongsTo) on the model has its return type updated to `BelongsTo<User, $this>` (per Pitfall 3 — column kept, target flipped, semantics carried by PHPDoc)
  </behavior>
  <action>
    Modify `app/Models/AdminActivityLog.php`:
    1. Find the `public static function log(...)` method. Replace the `Auth::guard('admin')->user()` call with `Auth::user()`.
    2. After fetching the user, add the guard per the verbatim excerpt in PATTERNS.md §"app/Models/AdminActivityLog.php": if `$user` is NOT a `User` instance OR `! $user->hasRole(Role::SuperAdmin)`, throw `\LogicException` with the exact message `'AdminActivityLog::log called outside super_admin context'` (string locked — the test asserts against this exact text).
    3. The `admin_user_id` column name stays (Pitfall 3 Option A) — the FK was re-pointed to users(id) by Plan 02 migration 5.
    4. Find any `admin()` BelongsTo relation method. Update its target class from `AdminUser::class` to `User::class` and the PHPDoc to `@return BelongsTo<User, $this>`. Optionally rename the method from `admin()` to `user()` — but per Pitfall 3 Option A, KEEPING `admin()` is the recommended path; documentation lives in the PHPDoc. (Choice: keep `admin()` and update PHPDoc only.)
    5. Add imports: `use App\Enums\Role;`, `use App\Models\User;`. Remove `use App\Models\AdminUser;` if present.
    6. Per CLAUDE.md "no fallback logic": do NOT add a backward-compat `Auth::guard('admin')` branch. Single source of truth.

    Implement ActivityLogTest (adapt existing — covers D-05 admin log invariant):
    - Acting as a seeded super_admin, call `AdminActivityLog::log('plan.updated', $plan, ['key' => 'val'])` → expect a new row, admin_user_id = super_admin user id.
    - Acting as a tenant Owner (no super_admin role), call `AdminActivityLog::log(...)` → expect `LogicException` thrown.
    - Unauthenticated (Auth::user() returns null), call `AdminActivityLog::log(...)` → expect `LogicException` thrown.
    - If any Admin controller already in the repo calls AdminActivityLog::log (search via grep), at least one feature test path that triggers that controller as super_admin → expect the activity log row to appear (regression check).
  </action>
  <verify>
    <automated>php artisan test --filter=ActivityLogTest --stop-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Auth::guard('admin')" app/Models/AdminActivityLog.php` equals 0 (legacy guard reference removed)
    - `grep -c "Auth::user()" app/Models/AdminActivityLog.php` ≥ 1
    - `grep -c "hasRole(Role::SuperAdmin)" app/Models/AdminActivityLog.php` ≥ 1
    - `grep -c "LogicException" app/Models/AdminActivityLog.php` ≥ 1 (guard exception)
    - `grep -rn "Auth::guard\\('admin'\\)" app/` returns 0 hits across the entire app/ tree (final cross-cutting verification)
    - ActivityLogTest exits 0
    - `./vendor/bin/phpstan analyse app/Models/AdminActivityLog.php --no-progress` reports 0 errors
  </acceptance_criteria>
  <done>AdminActivityLog::log no longer touches the deleted admin guard; super_admin-only invariant enforced via LogicException; ActivityLogTest passes; final cross-cutting grep confirms zero residual `Auth::guard('admin')` references anywhere in app/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request → Client controller action | Crossed by every authenticated tenant request; defended by `$this->authorize(Ability::X->value)` at action entry |
| signup form POST → User + UserRole | Crossed by anonymous registration; defended by transactional creation + `role` field stripped from User::$fillable + `role => 'owner'` hardcoded server-side |
| Admin controller action → AdminActivityLog::log | Crossed when admin mutates platform state; defended by Auth::user() + SuperAdmin role assertion in ::log() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.1.05-01 | Elevation of Privilege | mass-assignment of role via /register POST body | mitigate | Server-side hardcodes `role => Role::Owner` in UserRole::create regardless of request payload. Acceptance criterion (mass-assignment guard test) fails if a future change passes user input to the UserRole role field |
| T-16.1.05-02 | Elevation of Privilege | controller action with no authorize call (silent grant) | mitigate | AbilityCoverageTest walks every dashboard/* route and asserts the matrix-expected response code for each role. A new action without authorize would manifest as 200 for a role that should 403 — test failure |
| T-16.1.05-03 | Information Disclosure | DashboardController::index for no-role user | mitigate | Action calls `$this->authorize(Ability::ViewDashboard->value)`; a no-role user (theoretical post-revocation) gets 403 instead of seeing a partially-rendered dashboard |
| T-16.1.05-04 | Tampering | non-super_admin reaching AdminActivityLog::log | mitigate | LogicException thrown if Auth::user() lacks SuperAdmin; exception propagates to log/Sentry rather than silently writing a "tenant logged platform action" row |
| T-16.1.05-05 | Repudiation | partial signup (User created, UserRole insert fails) | mitigate | DB::transaction wraps Tenant + User + UserRole creation; rollback test (Task 2) verifies that no orphan User survives a UserRole insert failure |
| T-16.1.05-06 | Elevation of Privilege | Plan 02 left some isOwner/isManager/isAgent call sites in app/Http/Controllers/ | mitigate | Task 1 acceptance grep `grep -rnE "->isOwner|->isManager|->isAgent" app/Http/Controllers/Client/` must return 0; PHPStan baseline=0 catches any call to a deleted method at static-analysis time |
| T-16.1.05-07 | Tampering | bot_custom_instructions tenant-side editability (D-09) | mitigate | Plan 01's BotBrainInvariantTest greps Client FormRequests for bot_custom_instructions — this plan doesn't add any new Client FormRequests, but if it did, the guard test still applies. Existing Admin\ClientController:204 retains exclusive write access to the field |
</threat_model>

<verification>
```bash
php artisan test --filter='AbilityCoverageTest|RegisterControllerTest|ActivityLogTest'
php artisan test                                              # full suite — Plan 04 + 05 done, controllers green
./vendor/bin/phpstan analyse --no-progress                    # baseline = 0
./vendor/bin/pint --test app/Http/Controllers/ app/Models/AdminActivityLog.php
```

Final cross-cutting grep gates:
```bash
grep -rnE "->isOwner\\(\\)|->isManager\\(\\)|->isAgent\\(\\)" app/ | grep -v '^#' | wc -l   # must be 0
grep -rn "Auth::guard\\('admin'\\)" app/ | grep -v '^#' | wc -l                            # must be 0
grep -rn "AdminUser::class\\|use App\\\\Models\\\\AdminUser" app/ | grep -v '^#' | wc -l    # must be 0
```
</verification>

<success_criteria>
- 10 Client controllers carry matrix-mandated authorize calls
- RegisterController writes User + UserRole(owner) in one transaction; mass-assignment guarded
- AdminActivityLog::log reads Auth::user() + asserts SuperAdmin
- Cross-cutting grep: zero residual references to deleted helpers, the admin guard, or AdminUser
- AbilityCoverageTest + RegisterControllerTest + ActivityLogTest pass
- Full Pest suite green (Plans 04 + 05 complete; UI sweep in Plan 06 is the next blocker but the backend is now stable)
- PHPStan baseline = 0 maintained
- Pint clean
</success_criteria>

<output>
Create `.planning/phases/16.1-role-foundation/16.1-05-SUMMARY.md`. Required sections:
- Per-controller ability mapping confirmation (10 controllers × matrix abilities)
- RegisterController transactional flow (User + UserRole)
- AdminActivityLog rewrite
- Final cross-cutting grep results (3 zero-hit confirmations)
- Test status
- Known UI dependencies for Plan 06 (Inertia share, top-nav, Vue v-if sweep)
</output>
