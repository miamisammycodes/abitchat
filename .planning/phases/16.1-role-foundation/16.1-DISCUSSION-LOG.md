# Phase 16.1: Role Foundation - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in 16.1-CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-20
**Phase:** 16.1-role-foundation
**Areas discussed:** Role taxonomy, Permission matrix scope, Enforcement pattern, Backfill + seeder + UI exposure

---

## Role Taxonomy

### Q1: Which role taxonomy locks in for v1.1?

| Option | Description | Selected |
|--------|-------------|----------|
| owner / editor / viewer | Matches Phase 17 SC2 verbatim. Industry standard (GitHub, Linear, Notion). | |
| owner / manager / agent (status quo) | Current DB enum + helpers stay. Phase 17 SC2 gets rewritten. 'agent' fits chatbot product. | ✓ |
| owner / admin / member | Slack/Stripe naming. Distances from Phase 17 SC2 verbiage. | |

**User's choice:** owner / manager / agent (status quo)
**Notes:** Implication — Phase 17 SC2 in ROADMAP.md must be updated by the planner of Phase 17 from "(owner, editor, viewer)" to "(owner, manager, agent)" to match the locked taxonomy.

---

### Q2: What's the manager vs agent split?

| Option | Description | Selected |
|--------|-------------|----------|
| Agent = conversations + leads only; Manager = all content + bot config | Three-tier escalation: brand = owner, brain = manager, conversations/leads = agent. | ✓ |
| Agent = read-only; Manager = full editor (no billing) | Simpler matrix; agent role less useful. | |
| Tiered narrower: bot config gates at owner | Three narrower tiers; per-field gating. | |

**User's choice:** Agent = conversations + leads only; Manager = all content + bot config
**Notes:** Subsequently revised by Q9 — bot brain (`bot_custom_instructions`) is platform-admin only, NOT in the tenant hierarchy. Manager scope shrinks to KB content + integrations + analytics only.

---

### Q3: Hierarchical role checks or flat role-membership checks?

| Option | Description | Selected |
|--------|-------------|----------|
| Hierarchical: owner ≥ manager ≥ agent | isAtLeastManager(): role IN (owner, manager). Manager inherits agent perms. | ✓ |
| Flat role-set checks | Permission helpers enumerate explicit roles. More verbose. | |
| Ability-based via Gate::define | Gate::define('manage-bot-config', closure). Future-proof but adds abstraction. | |

**User's choice:** Hierarchical
**Notes:** Drove the helper-shape question (Q4) toward backed-enum hasRoleAtLeast().

---

### Q4: How should the tenant-role hierarchy be expressed on the User model?

| Option | Description | Selected |
|--------|-------------|----------|
| Add isAtLeastManager() / isAtLeastAgent(); keep exact-role helpers | Two helper families on User. Backward-compatible. | |
| Replace with hasRoleAtLeast(Role::Manager) using PHP backed enum | New App\Enums\Role with ordinal rank. Type-safe. Bigger refactor. | ✓ |
| Single $user->can($ability) backed by Gate-based role-rank map | Most semantic but eliminates exact-role checks entirely. | |

**User's first response:** "waht does owner mean, the whole system admin or tenant owner" — clarifying question. Reflected back: `users.role` is tenant-scoped (per-tenant role); platform admins live in separate `admin_users` table.
**User's choice (after clarification):** Replace isOwner/isManager/isAgent with hasRoleAtLeast(Role::Manager) using a PHP backed enum
**Notes:** Locked in `App\Enums\Role` backed enum.

---

## Permission Matrix Scope

### Q5: How wide should the role-gating sweep be in Phase 16.1?

| Option | Description | Selected |
|--------|-------------|----------|
| Minimal: trigger + adjacent tenant settings only | Owner-only: WebsiteIndexing + tenant settings + billing. | |
| Wide: lock every Client controller mutation | Every Client controller gains a Gate/FormRequest check. | |
| Tiered ship: foundation + trigger fix now; matrix per-controller follows | 16.1 ships foundation + trigger fix; subsequent PRs add controller checks. | ✓ |

**User's choice:** Tiered ship
**Notes:** Subsequently REVISED by Q12 — user explicitly chose "Ship all tenant UI gates in 16.1", which requires backend gates to ship simultaneously. Reconciled in CONTEXT.md D-14: full sweep (backend + frontend) in 16.1.

---

### Q6: Should Phase 16.1 ship a canonical permission matrix document?

| Option | Description | Selected |
|--------|-------------|----------|
| Ship matrix as code: App\Auth\RolePermissions class with const map + tests | PHP class with const arrays. Tests assert full map. PHPStan verifies. | ✓ |
| Ship matrix as docs only: PERMISSION-MATRIX.md | Markdown table. Risk: docs drift from code. | |
| Ship matrix in CONTEXT.md only; build RolePermissions class incrementally | CONTEXT.md lists intended matrix. 16.1 ships only enforced abilities. | |

**User's choice:** Ship matrix as code
**Notes:** Locks `App\Auth\RolePermissions` as the canonical mapping with full-map test.

---

### Q7: Which abilities are owner-only (not delegable to managers) in the initial 16.1 matrix?

| Option | Description | Selected |
|--------|-------------|----------|
| manage-billing | Billing pages stay owner-only. | ✓ |
| manage-team — Phase 17 surface | Future team management UI is owner-only. | ✓ |
| manage-tenant-settings | Tenant-wide settings including website-indexing trigger. | ✓ |
| delete-tenant | Permanent destructive action. Phase 22 surface. | ✓ |

**User's choice (multi-select):** All four selected.

---

### Q8: Bot configuration is the murkiest tier — where does it sit?

| Option | Description | Selected |
|--------|-------------|----------|
| Manager+: custom instructions, KB attach, context window | Brand = owner, brain = manager. | |
| Owner-only: ALL bot config (brain + brand) | Tightest control. Manager has no influence on how bot responds. | |
| Tiered inside bot config | Per-field gating. | |

**User's first response:** "only the system admin should be able to tweak the bot's brain" — free-text answer requiring clarification of "system admin".
**Clarification provided:** "system admin" = platform admin (`admin_users` guard), NOT tenant owner.
**User confirmed interpretation:** Interpretation A — platform admin (`admin_users` guard) only.
**Notes:** Locked in D-09. Bot brain stays out of tenant hierarchy entirely; matches existing codebase (no tenant-side route for `bot_custom_instructions`).

---

### Q9: More questions / next area for Permission matrix scope?

| Option | Description | Selected |
|--------|-------------|----------|
| Next area | Move to Enforcement pattern. | |
| More questions | Edge cases still open. | ✓ ("i will tell you what i want") |

**User's free-text follow-up:** "What i want is the system should have 4 roles, Super admin who will be the admin of the entire system, owner: tenant owner, Manager: tenant manager which be assigned by the tenant owner and tenant agent which will be assigned by both manager and and owner. So the admin dashboard has to be viewe by only the super admin users. But one user can have both super admin role and owner role."
**Implication:** Significant architectural pivot — unify `users` + `admin_users` into a single table with multi-role pivot support. This reopened scope to include full RBAC unification.

---

### Q10: How should multi-role storage be modeled so one user can be both super_admin and owner?

| Option | Description | Selected |
|--------|-------------|----------|
| Pivot table user_roles(user_id, role, tenant_id NULL) on users table only | Drop admin_users; merge AdminUser into User. Cleanest end state. Biggest refactor. | ✓ |
| Keep admin_users separate; add admin_users.user_id FK to link identities | Two tables stay; linked. Two guards remain. | |
| Keep one users table; add JSON column users.roles | Single table, single guard. Smallest schema delta. JSON_CONTAINS queries. | |

**User's choice:** Pivot table user_roles
**Notes:** This is the load-bearing architectural decision.

---

### Q11: Is the full pivot-table RBAC refactor part of Phase 16.1, or do we split?

| Option | Description | Selected |
|--------|-------------|----------|
| Keep it all in Phase 16.1 — one big foundation PR | ~1–2 weeks. Single coherent landing. No intermediate state. | ✓ |
| Split: 16.1 = enum + scaffold + trigger fix; 16.2 = unify guards | Faster unblock + safer staging. Recommended by Claude. | |
| Split differently: 16.1 = unify first; 16.2 = enum + matrix | Locks harder architecture first; trigger fix waits. | |

**User's choice:** Keep it all in Phase 16.1 — one big foundation PR
**Notes:** Acknowledged scope expansion. 16.1 is now a ~1–2 week phase.

---

### Q12: After merging admin_users into users, how does users.tenant_id behave?

| Option | Description | Selected |
|--------|-------------|----------|
| users.tenant_id stays, becomes nullable; super_admin = NULL; tenant roles still 1×1 | Smallest behavior shift. BelongsToTenant + DEC-05 keep working. Multi-tenant = Phase 17. | ✓ |
| Drop users.tenant_id entirely; ALL context in user_roles.tenant_id | Phase 17-ready. Maximum churn. | |
| Keep users.tenant_id as 'home tenant'; user_roles adds additional memberships | Hybrid. Two sources of truth. | |

**User's choice:** users.tenant_id stays, becomes nullable
**Notes:** Preserves the 1×1 invariant for tenant users in 16.1; multi-tenant-per-user is Phase 17 work.

---

## Enforcement Pattern

### Q13: What's the primary enforcement pattern for the 4-role system?

| Option | Description | Selected |
|--------|-------------|----------|
| Laravel Gates (Gate::define) + $this->authorize(...) everywhere | Single canonical surface. PHPStan-friendly. Plays well with existing patterns. | ✓ |
| Policies on virtual subjects | More Eloquent-native. Role-only abilities awkward without a model subject. | |
| Custom middleware + role-rank guards | Most explicit in routes. No central abilities registry. | |

**User's choice:** Laravel Gates everywhere

---

### Q14: After unifying the guards, how does login routing work?

| Option | Description | Selected |
|--------|-------------|----------|
| Single /login page; post-login redirect is role-aware | Cleanest end state. Dual-role users see chooser. | ✓ |
| Keep /admin/login as separate visual entry; both hit same users table | Preserves muscle memory + admin URL hygiene. | |
| Single /login; in-app context-switcher in nav | Stripe/GitHub Orgs UX. Net-new switcher component. | |

**User's choice:** Single /login with role-aware redirect

---

### Q15: More questions / next area for Enforcement pattern?

| Option | Description | Selected |
|--------|-------------|----------|
| Next area | Move to Backfill + seeder + UI exposure. | |
| More questions | Edge cases on Policies, route prefix, Gate caching. | ✓ |

---

### Q16: How are existing Eloquent policies updated for role-tiered access?

| Option | Description | Selected |
|--------|-------------|----------|
| Policies stack tenant-ownership + role-tier; abilities split into view/update/delete | Granular per-ability. Existing authorize() calls keep working. | ✓ |
| Policies stay ownership-only; role-gating moves to Gate::define at controller level | Easier to reason but more boilerplate. | |
| Single policy method that takes ability constant | Most unified but novel pattern. | |

**User's choice:** Policies stack tenant-ownership + role-tier

---

## Backfill + Seeder + UI Exposure

### Q17: How is the admin_users → users data migration handled (collision strategy)?

| Option | Description | Selected |
|--------|-------------|----------|
| Merge by email | One human + same email = same user record. Handles dual-role case. | |
| Insert all admin_users as fresh User rows; never merge | Safest data integrity but blocks dual-role case. | |
| Skip data migration; rebuild from scratch | Cleanest schema; needs manual re-seed in prod. | |

**User's free-text answer:** "since there is no prod for this system we can do everyting from the scratach"
**Resolution:** Effectively chose option 3 (rebuild from scratch). No production data to preserve. Drop `admin_users` entirely; rebuild via seeder.

---

### Q18: How are roles surfaced to Vue (Inertia share) so the frontend can hide/disable controls?

| Option | Description | Selected |
|--------|-------------|----------|
| Share resolved abilities: auth.user.can = { manage_billing: true, ... } | Flat map. No role-checking logic in Vue. | ✓ |
| Share raw roles: auth.user.roles = ['super_admin','owner']; Vue computes gates | Logic duplicated. Diverges over time. | |
| Share both: roles + can map | Defensive. Larger payload. | |

**User's choice:** Share resolved abilities (flat boolean map)

---

### Q19: Which UI gates ship in Phase 16.1 itself, vs deferred to follow-up PRs?

| Option | Description | Selected |
|--------|-------------|----------|
| Ship: admin route tree + Settings/WebsiteIndexing v-if; rest follows | Smallest 16.1 v-if surface. | |
| Ship all tenant UI gates in 16.1: v-if on every owner/manager/agent surface | Full sweep in 16.1. Coherent UX shift. | ✓ |
| Minimal: only WebsiteIndexing 403 fix + admin route gate; no v-if elsewhere | Smallest PR; ugly UX. | |

**User's choice:** Ship all tenant UI gates in 16.1
**Notes:** Reconciles with the earlier "tiered ship" answer (Q5) — both backend + frontend ship together in 16.1.

---

### Q20: What does the rebuilt DatabaseSeeder produce for local dev / test environments?

| Option | Description | Selected |
|--------|-------------|----------|
| 1 super_admin + 1 dual-role user + 1 owner + 1 manager + 1 agent across two tenants | Every role tier + dual-role edge case testable. | ✓ |
| Minimal: just the two existing accounts | Match current CLAUDE.md exactly. No manager/agent. | |
| Recommended + demo data with sample conversations/leads/KB items | Bigger seeder; nicer demo. Risk of stale fixtures. | |

**User's choice:** Recommended (full matrix seeder)

---

### Q21: For the dual-role super_admin+owner case, does the top nav show a role/context indicator?

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — small role badge next to user name; context-switch chip for dual-role on /admin | Cheap to ship. Reduces 'where am I' confusion. | ✓ |
| No badge in 16.1; defer role/context UX to Phase 17 | Smaller 16.1 visual scope. | |
| Yes badge + global 'You are: [Role] of [Tenant]' banner on every page | Heavyweight. | |

**User's choice:** Yes — role badge + dual-role context chip

---

## Claude's Discretion

The planner has flexibility on these (captured in CONTEXT.md `<decisions>` → Claude's Discretion):

- Whether `user_roles.tenant_id` consistency is enforced via DB CHECK or app-level only
- Exact column ordering and index choices on `user_roles`
- Migration ordering and rollback strategy (must work with `migrate:fresh --seed`)
- Whether the dual-role chooser is a full intermediate page or an inline modal
- Exact role-badge styling and chip placement
- Whether to invoke `/gsd:ui-phase` for the dual-role chooser + role badge design

## Deferred Ideas

(Full list in 16.1-CONTEXT.md `<deferred>` section.)

- Phase 17 surfaces: invite-by-email, SSO, ownership transfer, multi-tenant-per-user, conversation assignment
- Phase 16 surfaces: admin impersonation UI, system health, failed jobs, broadcast messaging, audit log
- Phase 22 surfaces: account deletion UI, 2FA integration
- Update Phase 17 SC2 in ROADMAP.md from "(owner, editor, viewer)" to "(owner, manager, agent)" during Phase 17 planning
- DB-level CHECK constraint for user_roles.tenant_id consistency (post-16.1 hardening if needed)
