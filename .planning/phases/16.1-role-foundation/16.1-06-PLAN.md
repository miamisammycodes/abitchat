---
phase: 16.1-role-foundation
plan: 06
type: execute
wave: 5
depends_on:
  - 16.1-04
files_modified:
  - app/Http/Middleware/HandleInertiaRequests.php
  - resources/js/Components/RoleBadge.vue
  - resources/js/Components/ContextSwitchChip.vue
  - resources/js/Pages/Auth/ChooseRole.vue
  - resources/js/Pages/Admin/Auth/Login.vue
  - resources/js/Layouts/AdminLayout.vue
  - resources/js/Layouts/ClientLayout.vue
  - resources/js/Pages/Client/Dashboard.vue
  - resources/js/Pages/Client/Widget/Index.vue
  - resources/js/Pages/Client/Leads/Index.vue
  - resources/js/Pages/Client/Leads/Show.vue
  - resources/js/Pages/Client/KnowledgeBase/Index.vue
  - resources/js/Pages/Client/KnowledgeBase/Create.vue
  - resources/js/Pages/Client/KnowledgeBase/Show.vue
  - resources/js/Pages/Client/KnowledgeBase/Edit.vue
  - resources/js/Pages/Client/Conversations/Index.vue
  - resources/js/Pages/Client/Conversations/Show.vue
  - resources/js/Pages/Client/Billing/Index.vue
  - resources/js/Pages/Client/Billing/DkQrSession.vue
  - resources/js/Pages/Client/Billing/Plans.vue
  - resources/js/Pages/Client/Billing/Subscribe.vue
  - resources/js/Pages/Client/Billing/Transactions.vue
  - resources/js/Pages/Client/Analytics/Index.vue
  - resources/js/Pages/Admin/Dashboard.vue
  - resources/js/Pages/Admin/Clients/Index.vue
  - resources/js/Pages/Admin/Clients/Show.vue
  - resources/js/Pages/Admin/Inquiries/Index.vue
  - resources/js/Pages/Admin/Plans/Index.vue
  - resources/js/Pages/Admin/Plans/Create.vue
  - resources/js/Pages/Admin/Plans/Edit.vue
  - resources/js/Pages/Admin/Transactions/Index.vue
  - resources/js/Pages/Admin/Transactions/Show.vue
  - resources/js/Pages/Admin/Logs/Index.vue
  - tests/Feature/Inertia/AuthShareTest.php
autonomous: true
requirements:
  - D-13
  - D-14
  - D-15
  - D-21

must_haves:
  truths:
    - "HandleInertiaRequests::share() exposes auth.user with id, name, email, tenant{id,name}, roles[], has_super_admin_role, has_tenant_role, primary_role{value,label}, and a flat can{} map keyed by snake_case ability names"
    - "auth.admin is REMOVED entirely from the share payload (replaced by auth.user.has_super_admin_role + roles[])"
    - "share() loadMissing('roles') on the authenticated user (per Pitfall 5 — no N+1)"
    - "buildAbilityMap loops Ability::cases(), snake_cases the slug, runs Gate::forUser($user)->check($ability->value) per ability"
    - "primary_role resolves to 'super_admin' on /admin/* and the highest tenant role on /dashboard/* (per UI-SPEC Inertia Share Contract)"
    - "RoleBadge.vue is a stateless wrapper around Badge with variant+icon lookup; consumes role + label props"
    - "ContextSwitchChip.vue renders an Inertia Link with directional copy per UI-SPEC Copywriting Contract"
    - "Pages/Auth/ChooseRole.vue renders 2 Cards (Platform Admin + Owner of {tenant}) with locked copy from UI-SPEC"
    - "Pages/Admin/Auth/Login.vue is deleted"
    - "AdminLayout.vue + ClientLayout.vue use auth.user (not auth.admin); RoleBadge + ContextSwitchChip slot into top nav per UI-SPEC Layout Integration Rules"
    - "Every Pages/Client/*.vue + Pages/Admin/*.vue surface with role-gated controls wraps them in v-if=\"$page.props.auth.user.can.X\" — controls hide entirely (R1), no v-show, no :disabled (R8)"
  artifacts:
    - path: "app/Http/Middleware/HandleInertiaRequests.php"
      provides: "Share payload with auth.user.can flat map + roles + primary_role"
      contains: "buildAbilityMap"
    - path: "resources/js/Components/RoleBadge.vue"
      provides: "Stateless role badge with icon + label"
      contains: "lucide-vue-next"
    - path: "resources/js/Components/ContextSwitchChip.vue"
      provides: "Dual-role context switcher"
      contains: "ArrowLeftRight"
    - path: "resources/js/Pages/Auth/ChooseRole.vue"
      provides: "Dual-role chooser page"
      contains: "Continue as Platform Admin"
  key_links:
    - from: "resources/js/Pages/Client/Widget/Index.vue and other role-gated pages"
      to: "$page.props.auth.user.can"
      via: "v-if directive on mutation controls"
      pattern: "v-if=\"\\$page\\.props\\.auth\\.user\\.can\\."
    - from: "app/Http/Middleware/HandleInertiaRequests.php"
      to: "Gate (registered in Plan 04)"
      via: "Gate::forUser($user)->check inside buildAbilityMap"
      pattern: "Gate::forUser\\(\\$user\\)->check"
---

<objective>
Ship the full UI sweep: rewrite `HandleInertiaRequests::share()` to emit the unified `auth.user.can` map; build the two new components (`RoleBadge`, `ContextSwitchChip`); ship the `ChooseRole.vue` page; update both layouts (`AdminLayout`, `ClientLayout`) to consume `auth.user` (not `auth.admin`) and slot in the new components per UI-SPEC; thread `v-if="$page.props.auth.user.can.X"` across every Client + Admin Vue page that exposes role-gated controls; delete the obsolete `Pages/Admin/Auth/Login.vue`.

Purpose: Backend gates (Plans 04 + 05) are now correct, but the UI still shows owner-only controls to managers and displays the legacy `auth.admin` shape. This plan completes the user-facing half of Phase 16.1 and is the visible payoff of the whole effort.

Output: 1 middleware rewrite, 2 new components, 1 new page, 1 deleted page, 2 modified layouts, 23 modified Vue pages (the v-if sweep), 1 implemented test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16.1-role-foundation/16.1-CONTEXT.md
@.planning/phases/16.1-role-foundation/16.1-PATTERNS.md
@.planning/phases/16.1-role-foundation/16.1-UI-SPEC.md
@.planning/phases/16.1-role-foundation/16.1-VALIDATION.md
@.planning/phases/16.1-role-foundation/16.1-04-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Backend prerequisites (already shipped): -->

From Plan 04 — Gates registered for every Ability case in AppServiceProvider::boot().

From Plan 01 — `App\Enums\Role::label()` returns 'Platform Admin' for SuperAdmin, 'Owner'/'Manager'/'Agent' for tenant roles.

<!-- The full share() shape per UI-SPEC "Inertia Share Contract": -->

```typescript
auth.user: {
  id: number
  name: string
  email: string
  tenant?: { id: number; name: string } | null
  roles: Array<{ role: string; label: string; tenant_id: number | null }>
  has_super_admin_role: boolean
  has_tenant_role: boolean
  primary_role: { value: string; label: string } | null
  can: { [ability_snake_case: string]: boolean }
}
// auth.admin: REMOVED ENTIRELY
```

<!-- buildAbilityMap pseudocode per RESEARCH Pattern 6: -->

```php
foreach (Ability::cases() as $ability) {
    $key = str_replace('-', '_', $ability->value);     // manage-billing → manage_billing
    $map[$key] = Gate::forUser($user)->check($ability->value);
}
```

<!-- Layout touchpoints per UI-SPEC "Layout Integration Rules": -->

| File | Line | Change |
|------|------|--------|
| AdminLayout.vue | 33 | Replace `admin` computed with `user` computed reading `page.props.auth?.user` |
| AdminLayout.vue | 141 | `{{ admin.name }}` → `{{ user?.name }}` |
| AdminLayout.vue | 142-144 | Replace `<Badge>{{ admin.role }}</Badge>` with `<RoleBadge :role="primaryRole.value" :label="primaryRole.label" />` |
| AdminLayout.vue | between 139-140 | Insert `<ContextSwitchChip v-if="user?.has_tenant_role" direction="to-tenant" :tenant-name="user.tenant?.name" />` |
| ClientLayout.vue | 152 | Insert `<ContextSwitchChip v-if="user?.has_super_admin_role" direction="to-admin" />` as first child of the right-side cluster div |
| ClientLayout.vue | 169 (BEFORE the user-name span) | Insert `<RoleBadge :role="primaryRole.value" :label="primaryRole.label" />` so reading order is avatar — badge — name — chevron |

<!-- The full per-page ability mapping for the v-if sweep (the artifact that prevents Plan 06 from becoming a scavenger hunt): -->

| Vue Page File | Role-Gated Controls | Required Ability Key |
|---------------|---------------------|----------------------|
| Pages/Client/Dashboard.vue | "Upgrade plan" CTAs, manage-team prompts | manage_billing (Owner), manage_team (Owner) |
| Pages/Client/Widget/Index.vue | "Save" + "Recrawl now" + brand color/welcome message form submit buttons | manage_tenant_settings (Owner) |
| Pages/Client/Leads/Index.vue | "Export" button, "Delete" buttons, bulk actions | manage_leads (Agent+) |
| Pages/Client/Leads/Show.vue | "Edit", "Delete", "Add note" buttons | manage_leads (Agent+) |
| Pages/Client/KnowledgeBase/Index.vue | "Add knowledge item" button, "Delete" rows, bulk re-embed | manage_knowledge_base (Manager+) |
| Pages/Client/KnowledgeBase/Create.vue | Submit button, file upload form actions | manage_knowledge_base (Manager+) |
| Pages/Client/KnowledgeBase/Show.vue | "Re-embed", "Delete" buttons | manage_knowledge_base (Manager+) |
| Pages/Client/KnowledgeBase/Edit.vue | "Save", "Delete" buttons | manage_knowledge_base (Manager+) |
| Pages/Client/Conversations/Index.vue | "Delete" buttons, bulk actions | manage_conversations (Agent+) |
| Pages/Client/Conversations/Show.vue | "Take over", "Delete" buttons | manage_conversations (Agent+) |
| Pages/Client/Billing/Index.vue | "Upgrade", "Cancel", "Reactivate" CTAs | manage_billing (Owner) |
| Pages/Client/Billing/DkQrSession.vue | "Submit", "Verify RRN" buttons | manage_billing (Owner) |
| Pages/Client/Billing/Plans.vue | "Switch plan" buttons | manage_billing (Owner) |
| Pages/Client/Billing/Subscribe.vue | "Confirm", "Subscribe" buttons | manage_billing (Owner) |
| Pages/Client/Billing/Transactions.vue | "Download invoice", "Refund request" links | manage_billing (Owner) |
| Pages/Client/Analytics/Index.vue | The whole page body (Manager+ tier) — render an empty-state per UI-SPEC R4 if can.view_analytics_full is false | view_analytics_full (Manager+) |
| Pages/Admin/Dashboard.vue | Nothing role-gated within /admin (every viewer IS super_admin by route gating); add NO v-if directives |
| Pages/Admin/Clients/Index.vue | (admin-only — no v-if needed; users without super_admin can't reach the route) |
| Pages/Admin/Clients/Show.vue | (admin-only — no v-if needed) |
| Pages/Admin/Inquiries/Index.vue | (admin-only) |
| Pages/Admin/Plans/Index.vue | (admin-only) |
| Pages/Admin/Plans/Create.vue | (admin-only) |
| Pages/Admin/Plans/Edit.vue | (admin-only) |
| Pages/Admin/Transactions/Index.vue | (admin-only) |
| Pages/Admin/Transactions/Show.vue | "Approve", "Reject" buttons — admin-only by route gating; no v-if needed |
| Pages/Admin/Logs/Index.vue | (admin-only — read-only) |

Note: Admin pages largely don't need `v-if` directives because the `/admin/*` route group is already gated by `RequireSuperAdmin` (Plan 03) — every user reaching these pages IS super_admin. The `v-if` sweep is essential on the Client side where Owner/Manager/Agent share routes but see different controls.

<!-- UI-SPEC requirements (must hold): -->
- R1: v-if (not v-show, not :disabled, not opacity-50)
- R2: layout collapses naturally
- R4: when ALL action controls collapse for a role, render the locked "No actions available" empty state (Pages/Client/Analytics/Index.vue is the canonical case)
- R7: read-only data displays are NEVER role-gated
- R8: forbid disabled-with-tooltip
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite HandleInertiaRequests::share() — auth.user.can flat map + roles + primary_role</name>
  <files>app/Http/Middleware/HandleInertiaRequests.php, tests/Feature/Inertia/AuthShareTest.php</files>
  <read_first>
    - app/Http/Middleware/HandleInertiaRequests.php (entire file — current share() at lines 41-75 with the `auth.admin` block to remove)
    - .planning/phases/16.1-role-foundation/16.1-PATTERNS.md §"app/Http/Middleware/HandleInertiaRequests.php"
    - .planning/phases/16.1-role-foundation/16.1-RESEARCH.md §"Pattern 6: HandleInertiaRequests::share() Resolved Ability Map" + §"Pitfall 5"
    - .planning/phases/16.1-role-foundation/16.1-UI-SPEC.md §"Inertia Share Contract"
  </read_first>
  <behavior>
    - share() loads $user = $request->user(); calls $user?->loadMissing('roles') once per request (no N+1)
    - auth.admin is removed entirely; auth.user includes id, name, email, tenant, roles[], has_super_admin_role, has_tenant_role, primary_role, can
    - can{} is a flat map keyed by snake_case ability names; each value is the boolean result of Gate::forUser($user)->check($ability->value)
    - primary_role on /admin/* paths resolves to { value: 'super_admin', label: 'Platform Admin' } when the user has super_admin; on /dashboard/* paths resolves to the highest tenant role { value, label } the user holds; falls back to null
    - has_super_admin_role = bool — user has any UserRole row with role=super_admin
    - has_tenant_role = bool — user has any UserRole row whose role is NOT platform-level
    - The existing share keys (tenant, ziggy, flash, usageWarnings, usageStats, dkBankEnabled, latest_crawl_session) are preserved
  </behavior>
  <action>
    Modify `app/Http/Middleware/HandleInertiaRequests.php`:

    1. In `share()`:
       - Replace the existing `$admin = Auth::guard('admin')->user();` with `$user = $request->user();` then `$user?->loadMissing('roles');` (Pitfall 5).
       - Compute `$tenant = $user?->tenant;` (unchanged from current behavior).
       - Compute `$hasSuperAdminRole = (bool) $user?->roles->contains(fn (UserRole $ur) => $ur->role->isPlatformLevel());`.
       - Compute `$hasTenantRole = (bool) $user?->roles->contains(fn (UserRole $ur) => ! $ur->role->isPlatformLevel());`.
       - Compute `$primaryRole` per UI-SPEC "Inertia Share Contract":
         * If `$user === null` → `null`.
         * If `$request->is('admin/*')` AND `$hasSuperAdminRole` → `['value' => 'super_admin', 'label' => 'Platform Admin']`.
         * Else if `$request->is('dashboard/*')` → pick the highest tenant role from `$user->roles` filtering out platform-level + sort by Role::rank() descending → emit `{ value: $role->value, label: $role->label() }`. Returns null if no tenant role.
         * Default to whichever single role the user holds (super_admin if only super_admin, otherwise highest tenant).
       - Build `$roles = $user?->roles->map(fn (UserRole $ur) => ['role' => $ur->role->value, 'label' => $ur->role->label(), 'tenant_id' => $ur->tenant_id])->all() ?? [];`.
       - Build `$can = $user ? $this->buildAbilityMap($user) : [];`.

    2. Replace the existing `auth` array:
       ```
       'auth' => [
           'user' => $user ? [
               'id'    => $user->id,
               'name'  => $user->name,
               'email' => $user->email,
               'tenant' => $tenant ? ['id' => $tenant->id, 'name' => $tenant->name] : null,
               'roles' => $roles,
               'has_super_admin_role' => $hasSuperAdminRole,
               'has_tenant_role'      => $hasTenantRole,
               'primary_role'         => $primaryRole,
               'can'                  => $can,
           ] : null,
       ],
       ```
       The `'admin' =>` key is removed entirely.

    3. Preserve every other key in share() exactly as-is: tenant (top-level), ziggy, flash, usageWarnings, usageStats, dkBankEnabled, latest_crawl_session.

    4. Add `private function buildAbilityMap(User $user): array` returning the flat can map:
       ```
       /** @return array<string, bool> */
       private function buildAbilityMap(User $user): array {
           $map = [];
           foreach (Ability::cases() as $ability) {
               $key = str_replace('-', '_', $ability->value);
               $map[$key] = Gate::forUser($user)->check($ability->value);
           }
           return $map;
       }
       ```

    5. Add imports: `use App\Enums\Ability;`, `use App\Enums\Role;`, `use App\Models\User;`, `use App\Models\UserRole;`, `use Illuminate\Support\Facades\Gate;`. Remove `use Illuminate\Support\Facades\Auth;` if unused after the rewrite.

    Threat note: T-16.1.06-03 (stale can map across requests). The share() function runs PER REQUEST — no caching. Per CLAUDE.md "no Octane-unsafe singletons", do NOT memoize the ability map across requests.

    Implement AuthShareTest (unskip Plan 01 — covers D-13):
    - Anonymous request → auth.user is null; auth.admin key does NOT exist
    - Owner of Tenant A → auth.user.can.manage_billing === true; can.manage_knowledge_base === true; can.view_admin_dashboard === false
    - Manager of Tenant A → can.manage_knowledge_base === true; can.manage_billing === false
    - Agent of Tenant A → can.manage_conversations === true; can.manage_knowledge_base === false; can.manage_billing === false
    - SuperAdmin → can.view_admin_dashboard === true; can.manage_billing === false (orthogonal)
    - Dual-role test@example.com (Owner + SuperAdmin): on /admin/dashboard → primary_role.value === 'super_admin'; primary_role.label === 'Platform Admin'; has_super_admin_role + has_tenant_role both true
    - Same user on /dashboard → primary_role.value === 'owner'; primary_role.label === 'Owner'
    - Assert count of can keys equals 13 (all Ability cases mapped)
    - Assert all can keys use snake_case (e.g. `manage_billing`, not `manage-billing`)
    - Assert N+1 protection: capture query count via DB::listen, hit the route as a user with 1 UserRole row, assert no more than 1 `select * from user_roles` query was emitted (loadMissing working)
  </action>
  <verify>
    <automated>php artisan test --filter=AuthShareTest --stop-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Auth::guard('admin')" app/Http/Middleware/HandleInertiaRequests.php` equals 0
    - `grep -c "loadMissing('roles')" app/Http/Middleware/HandleInertiaRequests.php` equals 1
    - `grep -c "buildAbilityMap" app/Http/Middleware/HandleInertiaRequests.php` ≥ 2 (declaration + call)
    - `grep -c "Gate::forUser" app/Http/Middleware/HandleInertiaRequests.php` equals 1
    - `grep -c "'admin' =>" app/Http/Middleware/HandleInertiaRequests.php | head` equals 0 (the auth.admin key is gone)
    - AuthShareTest exits 0
    - `./vendor/bin/phpstan analyse app/Http/Middleware/HandleInertiaRequests.php --no-progress` reports 0 errors
  </acceptance_criteria>
  <done>share() emits the unified auth.user payload with can map + roles + primary_role; auth.admin is removed; N+1 prevented via loadMissing; AuthShareTest passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship RoleBadge.vue + ContextSwitchChip.vue + ChooseRole.vue + delete Admin/Auth/Login.vue</name>
  <files>resources/js/Components/RoleBadge.vue, resources/js/Components/ContextSwitchChip.vue, resources/js/Pages/Auth/ChooseRole.vue, resources/js/Pages/Admin/Auth/Login.vue</files>
  <read_first>
    - resources/js/Components/ui/badge/Badge.vue (the primitive RoleBadge wraps — read entire file to understand the variant prop)
    - resources/js/Components/ui/button/Button.vue (Button asChild pattern used by ChooseRole)
    - resources/js/Components/ui/card/ (CardHeader, CardTitle, CardDescription, CardContent — used in chooser layout)
    - .planning/phases/16.1-role-foundation/16.1-PATTERNS.md §"resources/js/Components/RoleBadge.vue" + §"ContextSwitchChip.vue" + §"Pages/Auth/ChooseRole.vue"
    - .planning/phases/16.1-role-foundation/16.1-UI-SPEC.md §"Role Badge Color Mapping" + §"Component Inventory" + §"Chooser Page Layout" + §"Copywriting Contract"
  </read_first>
  <behavior>
    - RoleBadge.vue accepts `role` (string) + `label` (string) props; renders an existing `<Badge>` with variant + lucide icon per UI-SPEC mapping: SuperAdmin→outline+Shield, Owner→default+Crown, Manager→secondary+Users, Agent→outline+Headphones
    - ContextSwitchChip.vue accepts `direction` ('to-admin' | 'to-tenant') + optional `tenantName`; renders an Inertia `<Link>` styled as a chip with `ArrowLeftRight` icon; copy verbatim from UI-SPEC ("Switch to my workspace: {tenant}" / "Switch to admin dashboard")
    - Pages/Auth/ChooseRole.vue renders the 2-card chooser per UI-SPEC "Chooser Page Layout" with strings VERBATIM from Copywriting Contract; each card's submit button POSTs to `/login/choose` with the corresponding context value
    - resources/js/Pages/Admin/Auth/Login.vue is DELETED (UI-SPEC line 277 — bookmarks return 404, acceptable per no-prod-data CONTEXT)
  </behavior>
  <action>
    Create `resources/js/Components/RoleBadge.vue` per PATTERNS.md verbatim. `<script setup>` (per CLAUDE.md Vue 3 conventions). Defines `props: { role: String required, label: String required }`. Internal `variantMap` + `iconMap` per UI-SPEC table. Imports `Badge` from `@/Components/ui/badge`. Imports `Shield, Crown, Users, Headphones` from `lucide-vue-next`. Template renders `<Badge :variant="variant" class="text-xs font-semibold"><component :is="Icon" class="h-3.5 w-3.5 mr-1" />{{ label }}</Badge>`.

    Create `resources/js/Components/ContextSwitchChip.vue` per PATTERNS.md verbatim. `<script setup>`. Defines `props: { direction: String required, tenantName: String default null }`. Imports `Link` from `@inertiajs/vue3` and `ArrowLeftRight` from `lucide-vue-next`. Computes `target` ('/admin/dashboard' or '/dashboard') and `label` per UI-SPEC Copywriting Contract: if to-admin → 'Switch to admin dashboard'; if to-tenant → 'Switch to my workspace: {{ tenantName }}' (fallback 'Switch to my workspace' if tenantName is null/empty). Template renders the Inertia `<Link>` with the chip styling: `inline-flex items-center gap-1 rounded-full bg-muted px-3 py-1 text-xs font-semibold text-muted-foreground hover:bg-muted/80`.

    Create `resources/js/Pages/Auth/ChooseRole.vue` per UI-SPEC "Chooser Page Layout". `<script setup>` with Inertia useForm or plain `<Link>` for the submit actions. Props: `availableContexts: Array` (from controller; shape `[{id: 'admin'|'tenant', tenantName?: string}]`). Template structure:
    - `<div class="min-h-screen flex items-center justify-center bg-background">` outer
    - `<div class="max-w-3xl mx-auto px-4">` inner
    - H1 `<h1 class="text-2xl font-semibold leading-tight">Choose how to continue</h1>` (LITERAL string from Copywriting Contract)
    - Sub-copy `<p class="text-sm leading-relaxed mt-2">You hold multiple roles on this account. Pick a context for this session — you can switch any time.</p>` (LITERAL)
    - `<div class="grid grid-cols-1 md:grid-cols-2 gap-6 mt-8">` cards container
    - 2 `<Card>` blocks (loop over availableContexts). Each:
      * Platform Admin card: `<Shield class="h-8 w-8 text-foreground">` icon + `<CardTitle>Platform Admin</CardTitle>` + `<CardDescription>Manage tenants, plans, transactions, and platform-wide settings.</CardDescription>` + `<CardContent>` with a Button asChild wrapping a `<Link method="post" href="/login/choose" :data="{context: 'admin'}" as="button">Continue as Platform Admin</Link>` (the trailing ArrowRight icon per UI-SPEC)
      * Owner card: `<Crown class="h-8 w-8 text-primary">` icon + `<CardTitle>Owner of {{ tenantName }}</CardTitle>` (fallback to 'Owner of your workspace' if tenantName is empty per UI-SPEC interpolation rules) + description literal + Button posting `{context: 'tenant'}`

    Strings VERBATIM from Copywriting Contract — NO custom wording. Reference UI-SPEC's "Chooser Page Layout" diagram for visual structure.

    Delete `resources/js/Pages/Admin/Auth/Login.vue` per UI-SPEC line 277 + CLAUDE.md Legacy Code Cleanup.

    Note: tests/Concerns/SeedsRoleMatrix.php is already wired in Plan 02 Task 4 (after UserRole + User::hasRole ship). No trait modification in this task — Vue components only.
  </action>
  <verify>
    <automated>php artisan test --filter='GateRegistrationTest|LoginRedirectTest|ChooseRoleTest|AbilityCoverageTest|PolicyTest' --stop-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `resources/js/Components/RoleBadge.vue` exists with `<script setup>` + Badge + lucide imports
    - `resources/js/Components/ContextSwitchChip.vue` exists with Inertia Link + ArrowLeftRight
    - `resources/js/Pages/Auth/ChooseRole.vue` exists with the literal strings "Choose how to continue", "Continue as Platform Admin", "Continue as Owner"
    - `resources/js/Pages/Admin/Auth/Login.vue` does NOT exist
    - `grep -c "Super Admin" resources/js/` returns 0 hits (UI label is Platform Admin per UI-SPEC)
    - Tests that use SeedsRoleMatrix (GateRegistrationTest, LoginRedirectTest, ChooseRoleTest, AbilityCoverageTest, PolicyTests) all exit 0
    - `npm run build` (or vite build) exits 0 — no Vue parse errors
  </acceptance_criteria>
  <done>RoleBadge + ContextSwitchChip + ChooseRole.vue ship; Admin/Auth/Login.vue deleted; dependent tests (already using the Plan-02-wired trait) pass; npm build succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Layout updates + per-page v-if sweep across all 25 Client + Admin Vue surfaces</name>
  <files>
    resources/js/Layouts/AdminLayout.vue,
    resources/js/Layouts/ClientLayout.vue,
    resources/js/Pages/Client/Dashboard.vue,
    resources/js/Pages/Client/Widget/Index.vue,
    resources/js/Pages/Client/Leads/Index.vue,
    resources/js/Pages/Client/Leads/Show.vue,
    resources/js/Pages/Client/KnowledgeBase/Index.vue,
    resources/js/Pages/Client/KnowledgeBase/Create.vue,
    resources/js/Pages/Client/KnowledgeBase/Show.vue,
    resources/js/Pages/Client/KnowledgeBase/Edit.vue,
    resources/js/Pages/Client/Conversations/Index.vue,
    resources/js/Pages/Client/Conversations/Show.vue,
    resources/js/Pages/Client/Billing/Index.vue,
    resources/js/Pages/Client/Billing/DkQrSession.vue,
    resources/js/Pages/Client/Billing/Plans.vue,
    resources/js/Pages/Client/Billing/Subscribe.vue,
    resources/js/Pages/Client/Billing/Transactions.vue,
    resources/js/Pages/Client/Analytics/Index.vue
  </files>
  <read_first>
    - resources/js/Layouts/AdminLayout.vue (current state — lines 33, 141-144 + the right-cluster wrapper around line 139-140)
    - resources/js/Layouts/ClientLayout.vue (current state — lines 152, 161-171 — the avatar/badge/name cluster)
    - resources/js/Pages/Client/Widget/Index.vue (the canonical "Save" + "Recrawl now" page — exact button locations)
    - .planning/phases/16.1-role-foundation/16.1-UI-SPEC.md §"Layout Integration Rules" + §"v-if Sweep Visual Rules" (R1-R8)
    - .planning/phases/16.1-role-foundation/16.1-PATTERNS.md §"resources/js/Layouts/AdminLayout.vue" + §"ClientLayout.vue" + §"v-if Sweep" (the canonical pattern)
    - The per-page ability mapping table in this plan's `<interfaces>` block (the source of truth for which v-if goes where)
  </read_first>
  <behavior>
    - AdminLayout.vue: `admin` computed removed; `user` computed reads `page.props.auth?.user`; `primaryRole` computed reads `user.value?.primary_role ?? { value: 'super_admin', label: 'Platform Admin' }`; the legacy `{{ admin.name }}` becomes `{{ user?.name }}`; the legacy `<Badge>{{ admin.role }}</Badge>` becomes `<RoleBadge :role="primaryRole.value" :label="primaryRole.label" />`; a `<ContextSwitchChip v-if="user?.has_tenant_role" direction="to-tenant" :tenant-name="user.tenant?.name" />` is inserted in the right-side cluster
    - ClientLayout.vue: `user` computed reads `page.props.auth?.user`; `primaryRole` computed reads `user.value?.primary_role ?? { value: 'owner', label: 'Owner' }` (fallback for a user with no resolved primary role); a `<ContextSwitchChip v-if="user?.has_super_admin_role" direction="to-admin" />` is inserted as the first child of the right-side cluster (line 152); a `<RoleBadge :role="primaryRole.value" :label="primaryRole.label" />` is inserted BEFORE the user-name span (line 169) so the reading order is avatar — badge — name — chevron
    - Every Client page in the file list with role-gated mutation controls gets `v-if="$page.props.auth.user.can.X"` on those controls per the per-page mapping table
    - R1 enforced: NO `v-show`, NO `:disabled="!can.X"`, NO `opacity-50 pointer-events-none` patterns
    - R4 enforced: Pages/Client/Analytics/Index.vue wraps its entire chart/data region in `v-if="$page.props.auth.user.can.view_analytics_full"`; an `v-else` renders the locked empty state per UI-SPEC R5 ("No actions available" heading + body literally)
    - Read-only data tables / lists / detail panes (Conversations table rows, Leads list rendering) are NEVER role-gated (R7)
    - Admin pages are NOT modified — they're already gated by RequireSuperAdmin middleware; every viewer IS super_admin
  </behavior>
  <action>
    **Layouts (do these first):**

    Modify `resources/js/Layouts/AdminLayout.vue` per UI-SPEC "Layout Integration Rules" table. The exact line offsets shift based on prior edits; use grep to find anchors:
    - `grep -n "page.props.auth" resources/js/Layouts/AdminLayout.vue` to find the `admin` computed.
    - Replace it with `const user = computed(() => page.props.auth?.user);` and add a `primaryRole` computed.
    - `grep -n "{{ admin\\." resources/js/Layouts/AdminLayout.vue` to find the user-name display; replace `{{ admin.name }}` with `{{ user?.name }}`.
    - `grep -n "<Badge" resources/js/Layouts/AdminLayout.vue` to find the legacy badge; replace with `<RoleBadge :role="primaryRole.value" :label="primaryRole.label" />`.
    - Insert `<ContextSwitchChip v-if="user?.has_tenant_role" direction="to-tenant" :tenant-name="user.tenant?.name" />` immediately before the badge.
    - Add imports: `import RoleBadge from '@/Components/RoleBadge.vue'; import ContextSwitchChip from '@/Components/ContextSwitchChip.vue';`.
    - Remove the now-unused `Badge` import if it's not consumed elsewhere in the file.

    Modify `resources/js/Layouts/ClientLayout.vue` symmetrically:
    - Find the right-side cluster wrapper div (matches `flex flex-1 items-center justify-end`). Insert `<ContextSwitchChip v-if="user?.has_super_admin_role" direction="to-admin" />` as the FIRST child.
    - Find the user-name `<span>` (matches `{{ user?.name }}` or similar). Insert `<RoleBadge :role="primaryRole.value" :label="primaryRole.label" />` BEFORE it.
    - Add `user` + `primaryRole` computeds (per AdminLayout pattern).
    - Add the same imports.

    **Per-page v-if sweep (do these in any order — they're independent):**

    For each Client page in `<files>`, apply the per-page mapping table from `<interfaces>`:

    1. Open the page.
    2. Locate mutation controls (Buttons, form submits, action links — `<Button @click="upgrade">`, `<form @submit>`, `<Link method="post">`, etc.). Hard rule: if a button triggers a write operation that maps to an ability in the matrix, gate it.
    3. Wrap each gated control in `v-if="$page.props.auth.user.can.X"` where X is the snake_cased ability key from the per-page mapping.
    4. R1 hard rule: NEVER use `v-show`, `:disabled="!can.X"`, or `:class="{ 'opacity-50 pointer-events-none': !can.X }"`. If you see any of these patterns in the existing code, replace them with `v-if`.
    5. R7 hard rule: Read-only content (tables of leads, list of conversations, detail panes) STAYS visible regardless of role — only mutation controls hide.
    6. For Analytics/Index.vue (R4 special case): wrap the page body content in `v-if="$page.props.auth.user.can.view_analytics_full"`. Add a `v-else` block rendering the locked empty state per UI-SPEC R5:

       ```html
       <div v-else class="rounded-md border border-dashed bg-muted/30 p-6 text-center">
         <h3 class="text-sm font-semibold text-foreground">No actions available</h3>
         <p class="text-sm text-muted-foreground mt-1">Your role can view this page but not modify it. Ask your workspace owner if you need elevated access.</p>
       </div>
       ```

       (Per UI-SPEC R5 — strings VERBATIM, no icon, no CTA.)

    **Admin pages are NOT modified.** Per the interfaces table, every reader of `/admin/*` is super_admin (gated by RequireSuperAdmin middleware), so no in-page role gating is required.

    Critical grep guards (run after the sweep):
    - `grep -rnE "v-show=\".*can\\." resources/js/Pages/` returns 0 hits (v-if mandatory)
    - `grep -rnE ":disabled=\"!.*\\$page\\.props\\.auth\\.user\\.can\\." resources/js/Pages/` returns 0 hits (no disabled-with-tooltip pattern)

    No new test file for this task — the v-if sweep is verified by:
    1. Plan 07's role × ability matrix browser smoke (planner's discretion + manual D-20 walk-through)
    2. The AuthShareTest from Task 1 (asserts can map structure)
    3. The grep guards above (source assertions)
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -30 && grep -rnE 'v-show="\\.*can\\.|:disabled="\\!.*\\$page\\.props\\.auth\\.user\\.can\\.' resources/js/Pages/ resources/js/Layouts/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 (Vue + Vite parse all touched files)
    - `grep -rc "v-if=\"\\\$page\\.props\\.auth\\.user\\.can\\." resources/js/Pages/Client/` returns at least 10 (multiple Client pages have at least one v-if directive)
    - `grep -c "RoleBadge" resources/js/Layouts/AdminLayout.vue resources/js/Layouts/ClientLayout.vue` ≥ 2 (1 per layout)
    - `grep -c "ContextSwitchChip" resources/js/Layouts/AdminLayout.vue resources/js/Layouts/ClientLayout.vue` ≥ 2
    - `grep -rE "v-show=.*can\\." resources/js/` returns 0 hits (R1: no v-show on can map)
    - `grep -rE ":disabled=\"!.*can\\." resources/js/` returns 0 hits (R1 + R8: no :disabled binding on can map)
    - `grep -c "{{ admin\\." resources/js/Layouts/AdminLayout.vue` equals 0 (legacy `admin` computed references gone)
    - `grep -c "auth.admin" resources/js/` returns 0 hits (legacy `auth.admin` consumption gone everywhere in the Vue tree)
    - `grep -rn "No actions available" resources/js/Pages/Client/Analytics/Index.vue` returns ≥ 1 hit (R5 empty-state ships in the canonical R4 case)
    - `grep -rn "Super Admin" resources/js/` returns 0 hits (UI label is Platform Admin)
  </acceptance_criteria>
  <done>Both layouts use the unified auth.user payload with RoleBadge + ContextSwitchChip; every Client page has the matrix-mandated v-if directives; the AnalyticsIndex empty state renders for Agent role per R4/R5; the npm build is green; all grep guards pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server share() payload → browser | Crossed once per Inertia request; UI booleans are ADVISORY — the backend `$this->authorize()` (Plans 04/05) is canonical |
| Vue template `$page.props.auth.user.can` → DOM render | If can map is stale (caching bug), a tenant Manager could see Owner-only buttons. Acceptance: share() runs PER REQUEST, no caching |
| Inertia Link form post (chooser) → ChooseRoleController | Crossed by dual-role users picking a context; defended in Plan 03 by per-context role check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.1.06-01 | Information Disclosure | Vue can map exposes ability names | accept | The keys are stable URL slugs already visible in routes (e.g. `/dashboard/billing` reveals "billing"); knowing the ability key adds no actionable surface for a tenant agent. Backend `$this->authorize()` is the gate |
| T-16.1.06-02 | Tampering | client modifies `can.manage_billing = true` in dev tools | mitigate | UI booleans are ADVISORY — every mutation hits a backend controller that ALSO authorizes (Plan 05). Local tampering surfaces "button visible but click 403s" rather than data damage |
| T-16.1.06-03 | Elevation of Privilege | stale `auth.user.can` map after role revocation | mitigate | share() resolves the can map PER REQUEST via Gate::forUser — no caching. After a role revocation, the next page load reflects the new state. Plan 04's `auth.user.can` map is not memoized in any singleton. Acceptance criterion (Task 1): N+1 protection via loadMissing, NOT cross-request memoization |
| T-16.1.06-04 | Tampering | `:disabled="!can.X"` + tooltip pattern slipping in | mitigate | Acceptance grep `grep -rE ":disabled=\"!.*can\\." resources/js/` must be 0; R1/R8 of UI-SPEC explicitly forbid this pattern |
| T-16.1.06-05 | Information Disclosure | dual-role user accidentally seeing wrong tenant's data on chooser misclick | mitigate | ChooseRoleController (Plan 03) authorizes the requested context; UI is decorative. URL prefix (/admin vs /dashboard) is the source of truth for the active context, not a session key |
| T-16.1.06-06 | Spoofing | "Super Admin" label leaking into UI | mitigate | UI-SPEC Copywriting Contract requires "Platform Admin" everywhere; acceptance grep `grep -rn "Super Admin" resources/js/` must be 0 |
</threat_model>

<verification>
```bash
php artisan test --filter='AuthShareTest|GateRegistrationTest|LoginRedirectTest|ChooseRoleTest|AbilityCoverageTest|PolicyTest'
npm run build                                          # Vue + Vite parse clean
./vendor/bin/phpstan analyse --no-progress             # baseline = 0
./vendor/bin/pint --test app/Http/Middleware/HandleInertiaRequests.php
```

Per-page v-if sanity check:
```bash
grep -rc "v-if=\"\\\$page\\.props\\.auth\\.user\\.can\\." resources/js/Pages/Client/
grep -rc "RoleBadge\\|ContextSwitchChip" resources/js/Layouts/
```

R1 / R7 / R8 hard-rule guards (must all return 0):
```bash
grep -rE "v-show=\".*can\\." resources/js/
grep -rE ":disabled=\"!.*\\\$page\\.props\\.auth\\.user\\.can\\." resources/js/
grep -rE "opacity-50.*pointer-events-none.*can\\." resources/js/
```
</verification>

<success_criteria>
- HandleInertiaRequests::share() emits the unified auth.user payload (no auth.admin)
- buildAbilityMap produces 13 snake_case keys via Gate::forUser->check
- loadMissing('roles') prevents N+1
- RoleBadge.vue + ContextSwitchChip.vue + ChooseRole.vue exist with verbatim UI-SPEC copy
- Admin/Auth/Login.vue deleted
- Both layouts updated: legacy `admin` computed removed, RoleBadge + ContextSwitchChip slotted in
- All listed Client Vue pages have matrix-mandated v-if directives
- Analytics/Index.vue R4 empty state ships with UI-SPEC-locked copy
- npm build green
- All R1/R7/R8 grep guards return 0
- AuthShareTest passes
- PHPStan baseline = 0
- Pint clean
</success_criteria>

<output>
Create `.planning/phases/16.1-role-foundation/16.1-06-SUMMARY.md`. Required sections:
- HandleInertiaRequests rewrite: new auth.user shape + buildAbilityMap output (sample with 13 keys)
- 3 new Vue components + 1 deleted page
- Both layouts updated (with line offsets that the executor actually used — write them down so Plan 07 browser smoke can locate the elements)
- Per-page v-if applications (a row for each modified page: file → ability key(s) → count of v-ifs added)
- R1/R7/R8 guard grep results (zero hits confirmed)
- Test status
- D-20 manual browser smoke runbook (input for Plan 07)
</output>
