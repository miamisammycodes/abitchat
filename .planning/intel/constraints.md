# Constraints Intel

Generated by gsd-doc-synthesizer. Source: 6 SPEC documents.
Precedence: SPEC > DOC. SPEC constraints override DOC-sourced assumptions on same scope.

---

## CONS-01 — Prompt Injection: Structural Defense Only
source: docs/superpowers/specs/2026-05-11-prompt-injection-design.md
type: protocol
status: shipped
content: Defense against prompt injection uses structural isolation only. No regex filtering. Approach: XML-style delimiters + section reordering.
  - Delimiter tags: <operator_persona>, <knowledge>, <chunk>
  - Untrusted content wrapped via wrapUntrusted() helper
  - All untrusted content HTML-escaped via escapeForPrompt(): < → &lt;, > → &gt;
  - Truncation: mb_substr/mb_strlen; suffix U+2026 (…)

## CONS-02 — Prompt Injection: bot_custom_instructions Partial Trust
source: docs/superpowers/specs/2026-05-11-prompt-injection-design.md
type: protocol
status: shipped
content: bot_custom_instructions field is treated as partial-trust content only.
  - Permitted: persona/flavor text (name, tone, style)
  - Prohibited: structural prompt control, overriding system rules, injecting new sections
  - Max length reduced: 2000 → 1000 characters
  - Injected inside <operator_persona> block, sandboxed before <knowledge> and STRICT RULES

## CONS-03 — Prompt Injection: Knowledge Chunk Caps
source: docs/superpowers/specs/2026-05-11-prompt-injection-design.md
type: nfr
status: shipped
content: Per-chunk cap: 1500 characters. Maximum chunks per prompt: 5. Total KB context ceiling: 7500 characters.

## CONS-04 — Prompt Injection: Canonical Section Order
source: docs/superpowers/specs/2026-05-11-prompt-injection-design.md
type: protocol
status: shipped
content: LLM prompt MUST be assembled in this order (non-negotiable):
  1. bot-type
  2. tone
  3. lead-capture
  4. <operator_persona> (contains bot_custom_instructions)
  5. <knowledge> (contains RAG chunks, each in <chunk> tag)
  6. STRICT RULES (always last — position prevents override)

## CONS-05 — Medium-Backlog Security: ValidateWidgetDomain CORS Ownership
source: docs/superpowers/specs/2026-05-12-medium-backlog-design.md
type: protocol
status: shipped (PR #14)
content: CORS for widget API is owned exclusively by ValidateWidgetDomain middleware.
  - Preflight (OPTIONS) handled at top of middleware stack, before api_key check
  - No other CORS logic for widget routes
  - Allowed domains is closed-by-default: tenants with no allowed_domains get 403

## CONS-06 — Medium-Backlog Security: EmbeddingFakeFactory Test Helper
source: docs/superpowers/specs/2026-05-12-medium-backlog-design.md
type: api-contract
status: shipped (PR #15)
content: EmbeddingFakeFactory provides canonical fake embedding generation for tests. All test code requiring embeddings MUST use this helper, not raw array construction.

## CONS-07 — Medium-Backlog Security: BustsTenantUsageCache Trait
source: docs/superpowers/specs/2026-05-12-medium-backlog-design.md
type: protocol
status: shipped (PR #13)
content: Cache invalidation for tenant usage cache is owned by BustsTenantUsageCache trait.
  - Fires on every model `created` event automatically
  - Do not add explicit Cache::forget calls for tenant usage cache — this trait is the single source of truth
  - Applies to all usage-counted models

## CONS-08 — Architecture: BelongsToTenant Trait (API Contract)
source: docs/superpowers/specs/2026-05-14-architecture-deepening-design.md
type: api-contract
status: shipped (PR #18)
content: All tenant-scoped Eloquent models MUST use the BelongsToTenant trait.
  - Trait provides: scopeForTenant(Tenant|int $tenant), tenant() BelongsTo relation, boot hook auto-stamping tenant_id on `creating` event
  - Applied models: User, Transaction, KnowledgeItem, UsageRecord, EnterpriseInquiry, Lead, Conversation
  - Canonical read API: Model::forTenant($tenant) — use this, not where('tenant_id', ...)
  - NoRawTenantIdWhere Larastan rule enforces at CI — direct where('tenant_id', ...) calls gate the build

## CONS-09 — Architecture: PHPStan/Larastan Baseline = Zero
source: docs/superpowers/specs/2026-05-14-architecture-deepening-design.md
type: nfr
status: shipped (PR #23)
content: PHPStan baseline is drained to zero. New violations gate CI. Baseline file must remain empty.
  - Larastan patterns enforced:
    - Use $this not static in trait @return BelongsTo docblocks
    - @use HasFactory<XFactory> on every model using HasFactory
    - @template T of BaseClass for invariant-generic method params
    - Trust PHPStan type narrowing — drop defensive null checks on non-nullable typed values
    - Eloquent Model::where(...) is StaticCall not MethodCall — use CallLike with instanceof narrowing in rules
    - @property PHPDoc required for Larastan to honor enum casts

## CONS-10 — Architecture: KnowledgeItemWorkflow State Machine
source: docs/superpowers/specs/2026-05-14-architecture-deepening-design.md
type: api-contract
status: shipped (PR #19)
content: KnowledgeItem status transitions are enforced by KnowledgeItemWorkflow. Invalid transitions throw.
  - Status enum: Pending | Processing | Ready | Failed
  - markProcessing: from {Pending, Failed} only
  - markReady: from {Processing} only; auto-invalidates KnowledgeCache on success
  - markFailed(Throwable): from any non-Ready state
  - TextChunker class: DELETED — do not reference
  - KnowledgeCache owns cache key shape; never construct keys manually

## CONS-11 — Architecture: LeadScoring Full-Rescore Behavior
source: docs/superpowers/specs/2026-05-14-architecture-deepening-design.md
type: api-contract
status: shipped (PR #20)
content: LeadScoring::score(Lead, ?Conversation): int is the sole scoring entry point.
  - Behavior: full re-score from current state on every call — scores CAN DECREASE on update
  - LeadScoringService: DELETED — do not reference
  - Weights: pricing=25, demo=30
  - Temperature thresholds: Cold 0-30, Warm 31-60, Hot 61-100

## CONS-12 — Architecture: UsageTracker::canRecordUsage Semantics
source: docs/superpowers/specs/2026-05-14-architecture-deepening-design.md
type: api-contract
status: shipped (PR #21)
content: UsageTracker::canRecordUsage() is the canonical "may this tenant record more?" decision point.
  - Threshold: <=0 remaining (not ===0) — tightened from original implementation
  - CheckUsageLimits middleware delegates to this method; no inline limit logic
  - BustsTenantUsageCache fires on model `created` — do not add redundant Cache::forget in tests

## CONS-13 — DK Bank QR: Route and Endpoint Contract
source: docs/superpowers/specs/2026-05-15-dk-bank-qr-design.md
type: api-contract
status: shipped (PR #24)
content: DK Bank QR payment routes and their behavior:
  - GET /billing/dk-qr/{plan} — show QR code page (GET, not POST; serves image persistently)
  - GET /billing/dk-qr/{transaction}/status — poll endpoint (returns JSON status; frontend polls every 3s for 2min window)
  - POST /billing/dk-qr/{transaction}/verify-rrn — RRN manual entry for non-DK cross-bank payers
  - DK→DK: backend polls DK /v1/intra-transaction/status with dk_reference_no
  - Non-DK: user pastes RRN; server calls same endpoint
  - Transaction::approveAndActivate() is the canonical activation method — both paths use it
  - Killswitch: DK_BANK_ENABLED=false gates all DK routes

## CONS-14 — DK Bank QR: Frontend Polling Behavior
source: docs/superpowers/specs/2026-05-15-dk-bank-qr-design.md
type: protocol
status: shipped (PR #24)
content: Frontend polls /status endpoint every 3 seconds. Window: 2 minutes (40 polls). On success: redirect to billing page. On timeout: show manual RRN entry form.

## CONS-15 — Registration Wizard: 3-Step Structure
source: docs/superpowers/specs/2026-05-18-registration-wizard-and-site-scraping-design.md
type: protocol
status: shipped (PR #25)
content: Registration wizard is exactly 3 steps: Account → Company → Website (optional).
  - Step 3 (Website) is skippable — no URL required to complete registration
  - Website URL captured in step 3 triggers CrawlWebsiteJob(mode=initial)
  - Job dispatched to `crawls` queue (dedicated Redis queue)

## CONS-16 — Website Crawler: Caps and Politeness Rules
source: docs/superpowers/specs/2026-05-18-registration-wizard-and-site-scraping-design.md
type: nfr
status: shipped (PR #25)
content: Crawler MUST respect:
  - Max pages per crawl: 100
  - Max crawl depth: 3
  - Max page size: 5MB
  - Request rate: 1 req/sec (polite crawl)
  - robots.txt: strictly honored; Crawl-delay directive honored
  - Sitemap-first discovery: sitemap.xml → sitemap index recursion → fallback to anchor crawl
  - Daily refresh: diff-only via Last-Modified/ETag/content hash comparison
  - One KnowledgeItem per crawled page; crawl_session_id stored in metadata

## CONS-17 — Website Crawler: CrawlWebsiteJob Modes
source: docs/superpowers/specs/2026-05-18-registration-wizard-and-site-scraping-design.md
type: api-contract
status: shipped (PR #25)
content: CrawlWebsiteJob(tenant, mode) where mode is one of: initial | refresh | manual
  - initial: first crawl post-registration; full crawl within caps
  - refresh: daily diff-only; only re-indexes changed/new pages
  - manual: tenant-triggered full recrawl; archives previous session
  - Queue: `crawls` (dedicated; not `default`)
  - Relevant models: crawl_sessions, crawl_url_blocklist, knowledge_items

## CONS-18 — Widget Session Tokens: JWT Structure
source: docs/superpowers/specs/2026-05-19-widget-session-tokens-design.md
type: api-contract
status: shipped (PR #29)
content: Widget session tokens are JWT HS256, signed with APP_KEY.
  - Claims: iss (app URL), sub (SHA-256(api_key + APP_KEY)), aud (canonical origin), ip, iat, exp
  - TTL: 30 minutes
  - Storage: in-memory only in widget.js — never localStorage, never sessionStorage
  - Issued by: POST /api/v1/widget/init
  - Library: firebase/php-jwt
  - Dual-accept window: 7 days (WIDGET_SESSION_DUAL_ACCEPT=true default)

## CONS-19 — Widget Session Tokens: Verification Rules
source: docs/superpowers/specs/2026-05-19-widget-session-tokens-design.md
type: api-contract
status: shipped (PR #29)
content: RequireWidgetSessionToken middleware verifies:
  - Token signature valid (APP_KEY)
  - Token not expired
  - sub matches SHA-256(api_key + APP_KEY) for the requesting tenant (cross-tenant binding)
  - aud matches canonical origin of request
  - ip matches request IP
  - Any mismatch → 401 (client retries with new token via /init)
  - WIDGET_SESSION_DUAL_ACCEPT=true: also accept raw api_key-only requests during migration window
  - TrustProxies middleware MUST be configured before strict-mode cutover (see memory: widget_session_tokens_pr29.md)

## CONS-20 — Widget Session Tokens: Rate Limits (Per-IP)
source: docs/superpowers/specs/2026-05-19-widget-session-tokens-design.md
type: nfr
status: shipped (PR #29)
content: ThrottleWidgetPerIp middleware enforces:
  - Init endpoint (/widget/init): 10 requests/minute per IP
  - Message endpoint (/widget/chat): 30 requests/minute per IP
  - Daily cap: 5000 requests/IP/day (raised from 1000 for CGNAT/shared-NAT tolerance)
  - Breach: 429 Too Many Requests

## CONS-21 — Widget Session Tokens: Audit Logging
source: docs/superpowers/specs/2026-05-19-widget-session-tokens-design.md
type: protocol
status: shipped (PR #29)
content: Widget API events (init, message, reject) logged to widget_audit log channel.
  - Log channel: widget_audit (configured in config/logging.php)
  - Events: token issued, token rejected, rate limit hit
  - Fields: tenant_id, api_key_hash, origin, ip, event_type, timestamp
  - Known deferred: audit-log try/catch hardening is a medium-tier follow-up (see memory: widget_session_tokens_pr29.md)

## CONS-22 — Widget Session Tokens: Deferred Follow-ups (Not Constraints Yet)
source: docs/superpowers/specs/2026-05-19-widget-session-tokens-design.md
type: nfr
status: deferred
content: The following items from the widget session token spec are explicitly deferred to a future hardening PR:
  - audit-log wrapped in try/catch (currently unguarded)
  - api_key null guard in middleware chain
  - Octane race condition on token verification
  - Enum/value-object cleanup for token claims
  - Indexed api_key_hash column for lookup performance
  These are NOT current constraints but are tracked here for roadmap consumers.

## CONS-23 — Architecture: Larastan Rule — Eloquent CallLike Pattern
source: docs/superpowers/specs/2026-05-14-architecture-deepening-design.md
type: protocol
status: shipped
content: When writing Larastan/PHPStan rules that inspect Eloquent calls:
  - Model::where(...) resolves as StaticCall, NOT MethodCall
  - Use CallLike with instanceof narrowing to catch both
  - This lesson applies to all custom Larastan rules in the codebase

## CONS-24 — Architecture: whereHas vs whereIn for Trait Scopes
source: docs/superpowers/specs/2026-05-14-architecture-deepening-design.md
type: protocol
status: shipped
content: whereHas() closure params do not resolve trait-defined scopes in all contexts.
  - Use whereIn(column, Builder::subquery) instead when filtering by trait scope
  - Builder::whereIn('col', Builder::query) generates inline subquery (not two round-trips)
  - whereKey($id)->forTenant($tenant) is canonical find-by-id+tenant pattern

## CONS-25 — Architecture: dispatchAfterResponse Behavior
source: docs/superpowers/specs/2026-05-12-medium-backlog-design.md
type: protocol
status: shipped
content: dispatchAfterResponse() forces synchronous execution under ANY queue driver (including sync).
  - Do not assume async behavior in tests when using dispatchAfterResponse
  - Verify against vendor source before using in new code paths
  - This is a known Laravel behavior, not a bug

## CONS-26 — Architecture: Restore-Style Routes Pattern
source: docs/superpowers/specs/2026-05-12-medium-backlog-design.md
type: api-contract
status: shipped
content: Routes that restore soft-deleted models use {id} parameter with onlyTrashed()->findOrFail() pattern.
  - Do NOT use route model binding for restore routes (binding resolves non-deleted only)
  - Pattern: Route::patch('resource/{id}/restore', ...); Controller: Model::onlyTrashed()->findOrFail($id)

## CONS-27 — Architecture: Model Boot Hook isDirty Guard
source: docs/superpowers/specs/2026-05-12-medium-backlog-design.md
type: protocol
status: shipped
content: Model boot hooks that compute derived columns SHOULD guard with isDirty(source_column).
  - Prevents unnecessary recomputation and avoids side effects on unrelated saves
  - Pattern: if ($model->isDirty('source_field')) { $model->derived_field = compute(); }

## CONS-28 — Architecture: PHP 8.4 + SQLite + RefreshDatabase Warning
source: docs/superpowers/specs/2026-05-14-architecture-deepening-design.md
type: nfr
status: shipped (known issue)
content: PHP 8.4 + SQLite + escaped exceptions can cascade-wedge RefreshDatabase transactions.
  - If tests deadlock or produce unexpected state, check PHP version + DB driver combination
  - Primary DB in tests should be SQLite in-memory or MySQL; avoid mixing
  - This is a known PHP 8.4 + SQLite interaction, not a Laravel bug
